\documentclass[12pt]{article}
\usepackage[utf8]{inputenc}
\usepackage{amsmath}
\usepackage{amssymb}
\usepackage{graphicx}
\usepackage{hyperref}
\usepackage{setspace}
\usepackage[round]{natbib}
\usepackage{longtable}
\let\oldcite\cite
\renewcommand{\cite}[1]{\citep{#1}}
\doublespacing

\title{Physical Primitives of Mind: Modeling Consciousness via Symmetries and Landauer Erasure}
\author{Sebastian Bobadilla-Suarez\\
\texttt{sebastian.bobadilla.s@gmail.com}}
\date{\today}
\usepackage{lineno}

\begin{document}
\linenumbers

\maketitle

\begin{abstract}
We present a mathematical synthesis and formal Lean 4 verification demonstrating how cognitive architectures can emerge as attractor states from established physical primitives, without introducing new fundamental physics. Starting from the Poincaré group of spacetime symmetries and finite phase space, we construct a framework for predictive processing. Spontaneous symmetry breaking generates necessary topological boundaries, and finite phase space subjects these boundaries to Landauer's thermodynamic erasure. Agency arises when systems evolve along paths of least action, minimizing entropy production via internal structural resonance with external perturbations. Scaling this dynamic to macroscopic levels aligns with emerging paradigms in neuroscience—specifically classical electrodynamic fields (ephaptic coupling)—and Kuramoto phase synchronization, culminating in a topological fixed point: a unified ``Self.'' We state the claim narrowly, because the subject matter invites a wider reading than the work supports: no new physics is derived here. Every physical result the chain invokes is established---Picard--Lindelöf, Kibble's classification of topological defects, Landauer's erasure bound, Gibbs' inequality, Still's bound on dissipation by nonpredictive information, Barbalat's lemma, Sakaguchi's threshold, Banach's fixed point. What is new is that they are stated in one formal language, their consistency mechanically checked, and the points where the chain relies on stipulation rather than derivation located exactly. The development declares no axioms and every structure carrying physical content is inhabited by a witness; the synchronization step is proved from a dynamics for initial data inside an explicit basin and assumed outside it, and the identification of a global section of the probability sheaf with the unity of experience is a stipulation, marked as such throughout rather than absorbed into the derivation. The step from a coarse-grained coupling kernel to the brain's endogenous electromagnetic field is likewise an empirical commitment and not a deduction; we separate it from the coarse-graining theorem that precedes it, weaken it to a modulatory-but-integrative role, and give four conditions that would falsify it. The framework is nonetheless falsifiable in one place that is its own and no rival's: recovery from sudden awakening should follow a delayed sigmoid rather than an exponential, because the variable gating it is astrocytic volume on a timescale of seconds to minutes rather than cortical phase realignment on a timescale of milliseconds. As a corollary, we show why rigid von Neumann architectures (e.g., GPUs) are thermodynamically disadvantaged or structurally constrained from reaching these specific continuous thermodynamic attractors.
\end{abstract}

\section{Introduction}
The pursuit of a physical theory of consciousness typically involves searching for the biological correlates of mind or treating subjective experience as an abstract computational algorithm. This paper asks a more fundamental question: If we assume only basic physical primitives—continuous symmetries (the Poincaré group), geometry, and thermodynamics—is the emergence of a unified, perceiving entity a natural physical attractor? 

We propose a framework where the structural properties of consciousness are derived as physical attractor states. Establishing a deductive chain from basic principles, we demonstrate how continuous symmetries break to form boundaries, how those boundaries dissipate free energy as non-equilibrium steady states, and how their evolution along paths of least action results in physical resonance with their environment. This eventually scales into the macroscopic electromagnetic unity proposed by emerging field theories of mind. To avoid critiques of ``math theatre,'' we delineate between literal physical mechanisms (e.g., thermodynamic erasure via Landauer's principle, boundaries from spontaneous symmetry breaking) and theoretical mappings, utilizing Lean 4 formalization as our mathematical anchor. We adopt a perspective aligned with Russellian Monism: objective measurements of macroscopic electromagnetic fields constitute the third-person ``outside,'' while the system's internal physical deformation to reach structural resonance constitutes the first-person intrinsic experience.

\subsection*{What is new here, and what is not}
A framework paper about consciousness invites a particular suspicion, and it is better answered at the start than at the end. \textbf{No new physics is derived here.} Every physical result the chain invokes is established, and most of it is old: Picard--Lindelöf for the well-posedness of the coupled dynamics \cite{coddington1955}; Kibble's classification of topological defects by the homotopy of the vacuum manifold \cite{kibble1976}; Landauer's bound on the heat cost of erasure \cite{landauer1961}; Gibbs' inequality for the relative entropy \cite{cover2006}; Barbalat's lemma for the asymptotics of a dissipating trajectory \cite{barbalat1959}; the threshold $K_c = 2D$ for the noisy mean-field Kuramoto model \cite{sakaguchi1988}; the bound of Still, Sivak, Bell and Crooks relating dissipation to nonpredictive information \cite{still2012}; the identity of Kawai, Parrondo and Van den Broeck equating dissipated work with the relative entropy between forward and time-reversed ensembles \cite{kawai2007}; and Banach's fixed-point theorem \cite{banach1922}. A physicist reading this paper will recognize all of it and would establish the point in three paragraphs if we did not. Conceding it costs nothing.

What is new is of a different kind, and it is a smaller claim than a theory of consciousness usually makes. These results have been stated in one formal language, their mutual consistency has been mechanically checked, and the places where the chain relies on stipulation rather than derivation have been located exactly. The value of a machine-checked chain is not that any link is novel; it is that no link can quietly borrow from another, that a hypothesis cannot be smuggled in as a definition, and that the seams --- where physics stops and interpretation begins --- are fixed in the source rather than negotiable in the prose. Section~\ref{sec:soundness} reports what the check cost: three of five original axioms each proved \texttt{False}, from one shared root cause, and none of the three had been caught by reading.

The framework is not thereby unfalsifiable, and one of its predictions is its own rather than a rival's. Section~\ref{sec:prediction} argues that recovery from sudden awakening should follow a \emph{delayed sigmoid} rather than an exponential, because the variable gating recovery is astrocytic volume and cerebrospinal-fluid clearance, on a timescale of seconds to minutes, while the cortical rhythms whose realignment is usually taken to constitute the recovery move in milliseconds. That is a claim about functional form; it is fittable against data of a kind already collected; and a purely electrical account of the same recovery predicts a different form.

\section{Formalization in Lean 4}
A core contribution of this work is the verification of our deductive chain using the Lean 4 interactive theorem prover. This approach builds on recent efforts to formalize consciousness and brain semantics, such as the 'OnlyOne' system \cite{scherf2026} and sheaf-theoretic models of neural manifolds \cite{inoue2026}. It is necessary to explicitly distinguish which claims have been mathematically derived as theorems within the proof assistant, and which remain irreducible physical postulates. Table 1 maps the primary claims of this paper to their current formalization status.

\begingroup
\scriptsize
\singlespacing
\setlength{\LTcapwidth}{\linewidth}
\begin{longtable}{|p{0.26\linewidth}|p{0.46\linewidth}|p{0.17\linewidth}|}
\caption{Mapping of physical claims to Lean 4 formalization status. The development declares \textbf{no axioms}: \texttt{\#print axioms} on any result above reports only \texttt{propext}, \texttt{Classical.choice} and \texttt{Quot.sound}. Earlier drafts carried five axioms, three of them inconsistent; all five became class fields, so a postulate is an obligation on each instance rather than a global assertion (Section~\ref{sec:soundness}), and two of the five have since been eliminated entirely (Derivation~5). ``Theorem'' still does not mean ``physically established'': the postulates live in hypotheses and instance obligations, and every such class is inhabited by a witness in \texttt{Examples.lean}, so no row is vacuous.}\\
\hline
\textbf{Conceptual Claim} & \textbf{Lean 4 Implementation} & \textbf{Status} \\ \hline
\endfirsthead
\hline
\textbf{Conceptual Claim} & \textbf{Lean 4 Implementation} & \textbf{Status} \\ \hline
\endhead
Pointwise Vacuum Minimization & \texttt{pointwise\_vacuum\_of\_global\_min} (\texttt{Phase1}), potential energy defined as an integral; holds a.e., and a double-well witness proves the a.e.\ cannot be strengthened & Theorem \\ \hline
Landauer's Principle & Derived from statistical mechanics (\texttt{Phase3}); the axiom-free reversibility core becomes a heat bound via the field \texttt{StatisticalMechanics.heat\_eq} & Theorem + instance postulate \\ \hline
Symmetry Breaking implies Boundaries & Derived from pointwise potential minimization (\texttt{Phase1}); witnessed on a double well with a $\mathbb{Z}_2$ symmetry whose minimisation hypothesis is proved, not assumed & Theorem \\ \hline
Inevitability of Boundaries ($\pi_0$) & The domain-wall case of the Kibble mechanism (\texttt{Phase1}~\S3): on a connected substrate, a continuous field taking values in two different connected components of the vacuum manifold must leave the vacuum somewhere (\texttt{exists\_\allowbreak notMem\_\allowbreak of\_\allowbreak connected\-Component\-In}). Witnessed on the double well, whose vacuum manifold is computed to be exactly $\{-1,1\}$ and proved disconnected (\texttt{Examples}~\S11.1); the field class is non-empty and the forced wall is located. Higher homotopy ($\pi_1$ vortices, $\pi_2$ monopoles) available and not taken & Theorem \\ \hline
Nonpredictive information bounds dissipation & Still, Sivak, Bell and Crooks' bound is the field \texttt{PredictiveDissipation.\allowbreak still\_bound} (\texttt{Phase3\_Predictive\-Thermodynamics}). Everything downstream is derived: mutual information is defined from Mathlib's \texttt{klDiv}; the data processing inequality for the chain $X_t \to S_t \to S_{t+1}$ proves the nonpredictive information non-negative (\texttt{predictiveInfo\_\allowbreak le\_\allowbreak mutualInfo}), from which $W_{\mathrm{diss}} \ge 0$ follows rather than being assumed; a dissipation budget bounds wasted memory, and zero dissipation forces every retained bit to be predictive. Witnessed in \texttt{Examples}~\S18 by two systems over one two-bit law, one permitted to dissipate nothing and one forced to dissipate its whole memory, with a counterexample showing the no-back-action hypothesis is load-bearing. This \textbf{replaces} the earlier ``gradient descent on $\sigma$ drives $D_{KL} \to 0$'', which was an invalid inference & Theorem + instance postulate \\ \hline
Structural Resonance KL Bound & Gibbs' inequality proved; the bound is the field \texttt{StructuralResonance.kl\_bound} (\texttt{Phase3\_KLBound}). Retained: the postulate is consistent and yields $\sigma \ge 0$. No limit may be read off it & Theorem + instance postulate \\ \hline
Structural Resonance via Least Action & Gradient flow of $\sigma$ in coupling space (\texttt{Phase8}), linked to \texttt{entropy\_production\_rate}; finite substrates & Theorem \\ \hline
Phase-locking minimizes entropy production & For a \emph{reciprocal} coupling kernel, the phase-locked field has the least entropy production of \emph{any} field (\texttt{phase\_locked\_\allowbreak minimizes\_\allowbreak entropy\_\allowbreak of\_\allowbreak symm}, \texttt{Phase8}~\S2a). The mean-matching condition the earlier form assumed over the comparison class is derived, not imposed: the coupling term is antisymmetric under exchanging its two sites, so total drift is conserved across phase fields (\texttt{total\_\allowbreak drift\_\allowbreak eq\_\allowbreak of\_\allowbreak symm}). Reciprocity is necessary --- a one-way coupling on two sites strictly beats its own phase-locked state (\texttt{Examples}~\S16) & Theorem \\ \hline
Electrodynamic Discretization & Lebesgue-Bochner edge weights (\texttt{Phase2}) sitting on faces of the complex and partitioning the region's stress-energy; mesh refinement converges, witnessed on $[0,1)$. The scalar read off the stress-energy tensor is a marked modelling choice: the partition holds for any non-negative choice, and two instances differing only in it have different couplings (\texttt{Examples}~\S6) & Theorem + instance postulate \\ \hline
Coarse-graining of a symmetric coupling & \texttt{mesh\_\allowbreak refinement\_\allowbreak convergence} (\texttt{Phase2}): the discrete coupling energy is the midpoint rule over unordered edges (\texttt{sum\_sum\_mul\_of\_symm}) and converges to the continuous integral along regular refining triangulations. It is a statement about the \emph{energy functional}, not the dynamics; \texttt{ContinuousNeuralField} is posited, not produced, and the propagation-of-chaos gap between the finite Kuramoto system and its continuum limit is open & Theorem, narrower than the prose it supports \\ \hline
The field is the endogenous EM field & \textbf{Not formalized and not derived.} The identification of the coarse-grained kernel with cortical ephaptic coupling is the framework's central empirical commitment (Section~\ref{sec:scaling}). Supported as \emph{modulatory and integrative}, not as primary: measured endogenous fields of $1$--$5$ mV/mm shift spike timing by $1$--$3$ ms. Four falsification conditions are stated in the text & Empirical commitment \\ \hline
Frustration as memory capacity & \textbf{Outside the regime the theorems cover.} Frustration needs competing couplings; \texttt{phase\_locked\_\allowbreak minimizes\_\allowbreak potential}, \texttt{ThermodynamicCover.A\_pos} and \texttt{Phase4\_RotatingFrame}~\S7 all require positive coupling, the last with a uniform bound $0 < a \le A_{ij}$. Well-posedness, Lyapunov descent and \texttt{velocity\_\allowbreak sq\_\allowbreak tendsto\_\allowbreak zero} carry no sign hypothesis, so \emph{that} the motion stops is frustration-agnostic; \emph{where} it stops is not. The memory claim is scoped out of the chain rather than repeated & Recorded gap \\ \hline
Phase synchronization to Unity & Sheaf-theoretic gluing (\texttt{Phase5}) over a \texttt{ThermodynamicCover}, whose fields are now all local: the global section is constructed by \texttt{probability\_glue\_unique} and the former class field \texttt{sync\_to\_section\_eq} is a theorem about it. Witnessed by two patches built from different mass profiles, each section constructed over its own patch, whose glued state is neither of them (\texttt{Examples}~\S14). The cover's equilibrium hypothesis is no longer assumed on every instance: \texttt{ThermodynamicCover.of\-Convergent\-Trajectory} (\texttt{Phase5\_\allowbreak Equilibrium\-Bridge}) discharges it from \texttt{kuramoto\_\allowbreak tendsto\_\allowbreak global\_\allowbreak minimum}, and \texttt{Examples}~\S17.1 is a three-patch cover whose phase field is the limit of a trajectory started \emph{unsynchronized}. The overlap-agreement hypothesis is untouched & Theorem + instance postulate \\ \hline
Reflexive Topology (the Self) & Banach fixed point for a contracting self-prediction map (\texttt{Phase6}); metric, completeness and contraction are hypotheses, all three discharged on the three-site substrate (\texttt{Examples}~\S10), where global sections are proved to be exactly the finite measures on it, the metric is the uniform distance between their densities, and the self-prediction map contracts by exactly $1/2$ without being constant. The avatar's link to the field is the predicate \texttt{Is\-Restriction\-Resonance}, discharged on the same witness; under it a covering family of avatars determines the global state (\texttt{eq\_\allowbreak of\_\allowbreak avatar\_\allowbreak eq}), and without it the Self theorem is unchanged (\texttt{self\_\allowbreak of\_\allowbreak const\-Resonance}) & Theorem (witnessed) \\ \hline
Kuramoto Lyapunov descent & Descent chained to the minimum characterization by the rotating frame (\texttt{Phase4\_RotatingFrame}); identical frequencies & Theorem \\ \hline
Kuramoto dynamics run & The initial value problem is well posed on all of $\mathbb{R}$: the field is globally Lipschitz and globally bounded (\texttt{kuramotoField\_lipschitz}, \texttt{kuramotoField\_norm\_le}), and through every state at every instant passes exactly one trajectory, defined for all time (\texttt{kuramoto\_cauchy\_problem}). Along \emph{every} trajectory of a zero-frequency system the dynamic potential converges (\texttt{dynamic\_potential\_tendsto}) and the total dissipation $\int_0^\infty \sum_i \dot\theta_i^2$ is finite, equal to the potential's total drop (\texttt{dissipation\_integral\_tendsto}). The motion stops: Barbalat's lemma is proved here, Mathlib lacking it, and applied, so $\sum_i \dot\theta_i^2 \to 0$ along every trajectory (\texttt{velocity\_sq\_tendsto\_zero}). Where it stops is proved under an initial-data condition: if the phases start within a quarter turn and the potential excess starts below $a/2$, where $a>0$ bounds the coupling below, the trajectory converges to a phase-locked configuration that is a \emph{global} minimiser, with $r^2=1$ (\texttt{kuramoto\_tendsto\_global\_minimum}), by a Polyak--\L{}ojasiewicz estimate rather than a LaSalle principle. Unconditionally this is false --- splay and twisted states are equilibria --- so the condition cannot be removed. Witnessed on three sites, where no closed form exists (\texttt{Examples}~\S17) & Theorem (conditional) \\ \hline
Continuous vs. Rigid Topology & Fixed wiring support at matched resource (\texttt{Phase7\_Rigidity}), deriving the suboptimality Phase 7 assumed; typed continuum/discrete separation & Theorem \\ \hline
Critical Coupling Threshold ($K_c = 2D$) & Self-consistency equation (\texttt{Phase8\_SelfConsistency}): at or below $K_c$, threshold included, only $r = 0$ solves it; above $K_c$ \emph{exactly one} $r > 0$ does (\texttt{supercritical\_fixed\_point\_existsUnique}), so the non-negative solutions are $\{0\}$ below and $\{0, r\}$ above; that $r$ tends to $0$ as $K \downarrow K_c$ and increases strictly with $K$ (\texttt{coherent\_branch\_strictMono}); a solution is a density reproducing its own order parameter. Conditional on the von Mises stationary density, which is an input, and on no link to a trajectory & Theorem (conditional) \\ \hline
\end{longtable}
\endgroup

\subsection{Soundness of the formalization}
\label{sec:soundness}

Formalization is only as trustworthy as the axioms it rests on, and an axiom that is inconsistent silently makes every theorem downstream of it vacuous while leaving the build green. We report a case of exactly this, found by auditing our own development, because the failure mode is easy to reproduce and we have not seen it discussed in the applied-formalization literature.

Earlier drafts of this work declared five axioms. Three of them were individually inconsistent: each, on its own, proves \texttt{False}. All three shared one root cause --- an axiom that constrains a symbol it does not itself bind. Concretely:

\begin{itemize}
\item \texttt{spontaneous\_symmetry\_breaking\_pointwise\_min} took the potential-energy functional as an \emph{implicit} argument occurring only in its hypothesis \texttt{PotentialEnergy phi = V v0}. Instantiating that functional with the constant map $\lambda\_.\,V(v_0)$ discharges the hypothesis by \texttt{rfl} and yields the conclusion for an arbitrary field. With $V(v) = v^2$, $v_0 = 0$ and $\phi \equiv 1$ this proves $1 = 0$.
\item \texttt{landauer\_heat\_eq} equated the free class field \texttt{heat\_dissipation} with a bath-entropy difference \emph{for every instance} of the class. An instance with zero heat dissipation and a bath that gains one bit gives $0 = \log 2$.
\item \texttt{kl\_bound\_axiom} asserted $\sigma \ge D_{KL}(P\|Q)/\Delta t$ for \emph{all} $P$ and $Q$, while $\sigma$ is a single real fixed by a class field. Since $D_{KL}$ is unbounded above, a point mass against a uniform distribution refutes it.
\end{itemize}

The remaining two axioms, \texttt{phase\_invariant\_periodic} and \texttt{sync\_to\_section\_eq}, had the same shape --- global assertions pinning free fields of \texttt{LocalSectionSynchronization} --- and are refutable as soon as the probability presheaf admits two distinct global sections. (Both were demoted to class fields at the time; both have since been removed from the class altogether, for a separate reason given in Derivation 5.)

The correction is uniform, and we state it as a rule we now follow: \emph{a physical postulate that mentions a class field must be a field of that class, never a standalone axiom quantified over all instances.} A postulate should be an obligation each model discharges, not a global claim about every model. Recast this way, the development declares no axioms at all. Two of the five became genuine theorems --- pointwise vacuum minimization is now proved from $\int f = 0 \wedge f \ge 0 \Rightarrow f = 0$ a.e., and Gibbs' inequality was already one --- and the other three became instance obligations.

This makes non-vacuity the live question, since an uninhabitable class makes its theorems vacuous just as surely as an inconsistent axiom did. \texttt{Examples.lean} therefore constructs witnesses: a one-bit erasure system with a genuine bath that discharges \texttt{heat\_eq} and reproduces $\Delta Q = k_B T \ln 2$; a perfectly resonant system discharging \texttt{kl\_bound}; and a scalar field on a one-point spacetime discharging \texttt{ActionPrinciples}. Derivation 5's gluing structures are witnessed too, which was until recently the largest open gap: \texttt{Examples.lean} builds a \texttt{ThermodynamicCover} over a three-site substrate carrying the discrete topology and its Borel $\sigma$-algebra, covered by two \emph{overlapping} open patches whose intersection is a single shared site. The local sections are germ families of a phase-indexed finite measure $\mu_\theta = (1+\cos\theta)^{+}\,\delta_{\mathrm{mid}}$ pushed into the sheafification of the probability presheaf; the family is provably non-constant in $\theta$ (\texttt{phaseMeasure\_not\_const}), so the periodicity obligation of the constructor \texttt{LocalSectionSynchronization.of\-InvariantMeasure} is discharged by an argument about $\cos$ rather than satisfied trivially, and \texttt{thermodynamic\_equilibrium} is \emph{derived} from \texttt{phase\_locked\_minimizes\_potential} rather than assumed. \texttt{global\_section\_from\_thermodynamics} then fires on this instance and glues the two patch-local sections into a unique global one. Two further covers answer the follow-up question of how non-degenerate such a witness can be made. \texttt{Examples.lean}~\S13 has a negative half worth recording: the phase field can be made non-constant as a function, but not as a physical state, because \texttt{thermodynamic\_\allowbreak equilibrium} forces every instance to a phase-locked configuration. \texttt{Examples.lean}~\S14 is built the other way round, from two patches with independently chosen mass profiles that meet only on the site they share, and the section the gluing returns is neither of them. That is the same kind of result as \texttt{contracting\_\allowbreak implies\_\allowbreak const} in Derivation 6 --- a proof that the witness is as strong as the class permits, rather than a witness that could be improved by trying harder.

What the witnesses establish is that the classes are inhabitable, so the theorems about them are not vacuous. What they do not establish is that a cortex is such an instance. In the cover above the phase field is already constant --- every patch locked at $\theta = 0$ --- which is precisely what makes the equilibrium obligation dischargeable; getting a physical system into that configuration is the content of the informal dynamical argument, not of the Lean development.

The same audit turned up a defect of a different kind, worth recording because it is not an axiom problem and would not have been caught by \texttt{\#print axioms}. \texttt{mesh\_refinement\_convergence} --- the claim that discrete edge weights converge to the continuous energy as the mesh is refined --- sat in a ``Conjectures'' section as an unproved \texttt{def \ldots : Prop}. It was not merely unproved but false. Nothing required the edge regions to cover the manifold, and $\mathrm{diam}\,\emptyset = 0$, so the triangulation whose edge regions are all empty satisfied the mesh hypothesis while contributing nothing to the sum, forcing $|0 - \int f| < \varepsilon$ for every $\varepsilon > 0$. Its binder was dead as well: it quantified over a triangulation and then resolved the vertex type by instance search rather than through the bound variable, so the variable was never used. \texttt{Phase2\_MeshConvergence.lean} replaces it with a theorem. Convergence is now stated along a \emph{sequence} of triangulations of a named region $S$, each required to be regular --- edge regions measurable, without self-loops, disjoint across distinct edges, covering $S$, and anchored to the simplicial data in the sense that a vertex lies in the closure of every region of an edge at it. Closure rather than membership is forced: cells are disjoint, so a shared vertex belongs to at most one of them. The manuscript's discrete energy $\tfrac12\sum_u\sum_v w(u,v) f(u)$, which samples only the first endpoint and is therefore not symmetric, is shown to equal exactly the midpoint rule $\sum_{u<v} w(u,v)\,\tfrac12(f(u)+f(v))$ over unordered edges; without that identification, reading the double sum as a Riemann sum would be a category error. At fixed mesh the error is at most $\varepsilon\,\mu(S)$ with $\varepsilon$ a modulus of continuity of the integrand, and convergence follows for uniformly continuous integrands as the mesh shrinks. The regularity conditions pull against each other, so \texttt{Examples.lean}~\S6 discharges them with the uniform partition of $[0,1)$ into half-open cells and checks that the approximation genuinely moves --- $\tfrac12$ at $N=1$ and $\tfrac14$ at $N=2$ for the tent function $x \mapsto |x - \tfrac12|$ --- rather than converging trivially. What is proved is convergence, not the $O(1/N^2)$ rate the accompanying simulation measures; that would need a midpoint error term with a second derivative, which we have not developed.

A third defect was one of bookkeeping across files, and it is the kind that survives any number of green builds because each half is individually correct. The development carried \emph{two} Kuramoto potentials. Lyapunov descent along the dynamics, $\dot V = -\sum_i \dot\theta_i^2$, was proved for the full potential $V(\theta) = -\tfrac12\sum_{i,j}A_{ij}\cos(\theta_i-\theta_j) - \sum_i \omega_i\theta_i$, while the characterization of the minimum --- phase-locked in both directions --- and everything downstream of it, including Derivation 5's gluing, concerned the coupling term alone. Nothing chained them, and the gap was not cosmetic: \texttt{kuramoto\_potential\_unbounded\_below} now proves that as soon as one $\omega_i \ne 0$ the full potential is unbounded below --- walk $\theta$ out along $\omega$ and the bounded cosine sum cannot compensate --- so it has no minimum for phase-locking to attain, and the sentence ``the phase-locked state minimizes the Lyapunov potential'' was, read literally, false of the functional the descent theorem was about. \texttt{Phase4\_RotatingFrame.lean} closes this by formalizing the standard reduction $\theta_i \mapsto \theta_i - \Omega t$. For identical natural frequencies $\omega \equiv \Omega$, a trajectory maps to a trajectory of the zero-frequency system --- which is exactly the system Derivation 5 already worked with --- the two potentials coincide there, and the descent result transports onto the coupling potential whose minimizers are the phase-locked states. Phase differences, phase-locking and the magnitude of the order parameter are all proved invariant under the change of frame, so nothing observable is lost in the reduction. \texttt{Examples.lean}~\S7 runs the chain end to end on two oscillators with a common frequency, discharging every hypothesis. The restriction is real and we state it plainly: the reduction is exact only for identical frequencies. A genuine spread of natural frequencies leaves residual detunings in the reduced system, phase-locking then requires coupling above a critical $K_c$, and $K_c$ is nowhere in our Lean development --- it is studied numerically, as Table 1 records.

A fourth defect was dimensional, and it concerned the one row of Table 1 that is still numerical rather than proved. \texttt{Phase8\_ContinuousField.lean} defines the critical coupling $K_c = 2D$ and a predicate for being above it. That predicate compared $\int_x\int_y K(x,y)$ against $2D$ --- but a double integral against an unnormalized measure carries the substrate's mass twice over, so a constant kernel $K$ on a substrate of total mass $m$ contributed $K m^2$. The comparison therefore depended on how large the substrate was rather than on how strongly it was coupled, and any kernel whatever could be pushed above threshold by inflating $m$. \texttt{Examples.lean}~\S9 exhibits the failure concretely: on a two-site substrate carrying counting measure, a system coupled at $K = 1/2$ against a threshold of $K_c = 1$ --- a factor of two \emph{below} threshold --- had an unnormalized double integral of $2 > 1$ and so satisfied the old predicate. The fix is to require the substrate's measure to be a probability measure, which is the normalization in which the mean-field derivation of $K_c = 2D$ is carried out; \texttt{mean\_field\_coupling\_const} then gives that a constant kernel has mean-field strength exactly $K$, and \texttt{exhibits\_phase\_transition\_const\_iff} reduces the predicate to $K > 2D$. We flag what this does and does not do. It makes the predicate say something whose truth depends only on the physics, and \S9 witnesses it on both sides of the threshold. It does not by itself prove $K_c = 2D$: the content of that value is the bifurcation of the self-consistency equation $r = I_1(Kr/D)/I_0(Kr/D)$ at $K = 2D$, which is a separate question from whether the predicate is dimensionally sound. That bifurcation is proved below, in both directions, from an assumed stationary density; deriving the density itself needs the Fokker--Planck operator and is not done.

We draw no grand moral from this beyond the obvious one. The value of a proof assistant here was not that it certified our argument, but that it made a specific, checkable claim about what had been assumed --- and that claim turned out to be false in a way no amount of reading the prose would have revealed.

\section{Axiom 1: Symmetries and Phase Space}
Our starting premise relies on fundamental geometry. In our Lean formalization (\texttt{Phase1\_Primitives.lean}), continuous spacetime symmetries (e.g., the Poincaré group) act upon field states, and the invariance of the system's total energy under that action is recorded by the class \texttt{SymmetryInvariantAction}. Physically, it is such an invariance that Noether's theorem converts into the conservation of energy and momentum. The scope of the \emph{formal} claim is narrower and we state it plainly: the invariance is a class field, i.e.\ an assumption a model must discharge, and no conserved quantity is ever constructed from it. There is no Noether theorem in our Lean, and nothing downstream of this section depends on one. The class is now inhabited --- \texttt{Examples.lean}~\S11 exhibits a double-well potential $V(v) = (v^2-1)^2$ on a two-point spacetime with the $\mathbb{Z}_2$ action $v \mapsto \pm v$, discharges the invariance because $(\pm v)^2 = v^2$, and proves the vacuum manifold $\{-1,1\}$ degenerate with the two minima exchanged by the symmetry, so an energy-minimising field and its image have equal energy and differ. That is symmetry breaking exhibited rather than assumed; it is still not a conserved quantity, and $\mathbb{Z}_2$ being discrete there is no one-parameter family to differentiate along in the first place. Simultaneously, discrete permutation symmetries dictate the statistical mechanics of any physical system, defining a strictly finite volume of phase space.\footnote{While the Poincaré group and discrete permutations serve as accessible physical examples, the underlying Lean formalization does not hardcode specific physics. It defines the system at a more primitive mathematical level, relying entirely on generic Pseudo-Riemannian manifolds (for spacetime) and finite measure theory (for statistical mechanics). What the formalization supplies is the vocabulary in which symmetries, conserved quantities and phase-space constraints can be \emph{stated}; it does not derive them. All three of the classes involved are now inhabited in \texttt{Examples.lean} --- the $\mathbb{Z}_2$ action and its invariance in \S11, the real line with the Euclidean metric in \S12 --- but the instances are witnesses of inhabitability, not of physics: no theorem in the development consumes a symmetry group or a metric, so this section still contributes vocabulary rather than content.}

Because phase space is finite, any localized physical system possesses a mathematical limit on the amount of information (distinguishable states) it can embody at any given moment.

\section{Derivation 1: Symmetry Breaking and the Inevitability of Boundaries}
A perfectly symmetric universe is the uniform vacuum; no distinct entities exist. For any dynamic process to occur, the continuous symmetries of the background field must undergo spontaneous symmetry breaking. 

When a field drops to a lower, asymmetric energy state, topology dictates the natural creation of defects (such as domain walls or monopoles). This constraint necessitates the creation of a physical boundary—a localized topological distinction between an ``inside'' and an ``outside.'' While such a defect (e.g., a crack in a crystal lattice) is necessary to define a separate entity, it is not sufficient for agency. The boundary is relational, defined by the twisted geometry of the field surrounding it, and must be coupled with non-equilibrium thermodynamics to exhibit active behaviors.

That first sentence was, until recently, the one claim of this section that the formalization did not make. What Lean established was the energy half --- an energy-minimizing field sits in the vacuum manifold almost everywhere (\texttt{pointwise\_\allowbreak vacuum\_\allowbreak of\_\allowbreak global\_\allowbreak min}) --- together with the observation that a boundary defect \emph{obstructs} extension over a contractible interior. Neither produces a defect, so ``topology dictates the natural creation'' was asserted rather than proved. It is now the theorem \texttt{exists\_\allowbreak notMem\_\allowbreak of\_\allowbreak connected\-Component\-In}: if the substrate is connected and a continuous field takes values in two \emph{different connected components} of the vacuum manifold $M$, then somewhere the field is not in $M$ at all. That excluded region is the wall. This is the $\pi_0$ case of Kibble's classification of topological defects by the homotopy of the vacuum manifold \cite{kibble1976}, and the proof is elementary in exactly the way it is elementary in the physics literature: the continuous image of a connected space is connected, a connected subset of $M$ lies inside one component of $M$, and the field's range would be such a subset if it never left $M$. No hypothesis mentions energy, a potential, or a symmetry; connectedness of the substrate and disconnectedness of the vacuum are the whole of it.

We formalize the $\pi_0$ case and stop there. The higher cases --- $\pi_1 \ne 0$ forcing vortex lines, $\pi_2 \ne 0$ forcing monopoles --- are the rest of Kibble's classification and Mathlib's homotopy-group library would support at least the next one; they are available and not taken, because the argument this paper runs uses domain walls and nothing else. The witness is the double well of \texttt{Examples.lean}~\S11.1, where the vacuum manifold is \emph{computed} to be exactly $\{-1, 1\}$ (\texttt{wellVacuum\_\allowbreak eq}) rather than merely known to contain both minima, and its disconnection is proved in the form the theorem consumes: no preconnected subset of it contains both, since a preconnected subset of $\mathbb{R}$ is an interval and an interval spanning $-1$ and $1$ contains $0$, which is the top of the barrier. \texttt{wellV\_\allowbreak domain\_\allowbreak wall} then quantifies over \emph{every} continuous field on \emph{any} connected substrate that reaches both minima, with no formula for the field anywhere in the statement. That the class is non-empty is checked separately, because a theorem quantified over all fields is worthless if none satisfies its hypotheses: the clipped identity on $\mathbb{R}$ does, and the wall it is forced to have sits at the origin (\texttt{kink\_\allowbreak zero\_\allowbreak notMem}).

\section{Derivation 2: The Thermodynamic Constraint}
This newly formed topological boundary is subjected to gauge perturbations from the ``outside'' environment. Because the internal phase space is finite (Axiom 1), it cannot absorb an infinite sequence of incoming perturbations and must overwrite old internal states.

By Landauer's Principle \cite{landauer1961}, the erasure of physical information irreversibly decreases internal entropy, mandating that the system exhaust heat into the environment. Philosophers of physics (such as John Norton \cite{norton2005} and Orly Shenker \cite{shenker2000}) have critiqued Landauer's principle, arguing that physicists conflate \textit{logical} erasure (a many-to-one mapping in abstract computational states) with \textit{thermodynamic} erasure (a many-to-one mapping in physical phase space). They argue that logical erasure could conceptually be implemented by an injective physical process without dissipating heat. 

Our Lean 4 formalization (\texttt{Phase3\_CombinatorialThermodynamics.lean}) addresses this critique by defining erasure as a non-injective mapping on the \textit{physical} state space ($\neg \text{Function.Injective } t$). We formalize the global evolution of the bipartite universe (System + Bath) as strictly injective, representing Liouville's theorem. Lean verifies that heat dissipation is not tied to abstract logical erasure, but is a mandatory mathematical consequence of any local physical state space contraction under globally reversible dynamics. If a cognitive system's internal topology forces a contraction of its physical state space to accommodate structural resonance, heat dissipation becomes a thermodynamic requirement. A stable boundary thus constitutes a dissipative structure \cite{prigogine2018}, operating as a non-equilibrium steady state that consumes free energy to pay the thermodynamic cost of erasure. Continuous thermal fluctuations play a constructive role in establishing boundaries between states while contributing to the system's flexible, adaptive response.

\section{Derivation 3: Prediction as the Thermodynamic Cost of Memory}
A dissipative structure is driven by its environment. Write $X_t$ for the state of the structure and $S_t$ for the environmental signal driving it, and let the signal evolve by a dynamics of its own, $S_t \to S_{t+1}$, on which the structure does not act. Two quantities are then defined at each step: the \textit{memory} $I(X_t ; S_t)$, the mutual information between the structure's present state and the signal's present value, and the \textit{predictive information} $I(X_t ; S_{t+1})$, which is what that same state says about the signal's next value.

\paragraph{What this section used to claim, and why it is withdrawn.}
Earlier versions of this derivation argued as follows. Model the environment as a stochastic driving force $P(ext)$ and the structure's internal transitions as $Q(int)$; entropy production $\sigma$ is then bounded below by $D_{KL}(P || Q)/\Delta t$; the principle of least action drives $\sigma$ downward; therefore $D_{KL}(P || Q) \to 0$, and the internal transition probabilities come to mirror the statistical symmetries of the environment. The final step does not follow. Rearranged, the bound reads $D_{KL}(P || Q) \le \Delta t\,\sigma$, so descending on $\sigma$ lowers a \textit{bound} on the divergence; but a driven system does not relax to $\sigma = 0$. It relaxes to a non-equilibrium steady state at strictly positive entropy production, and the bound then yields only $D_{KL} \le \Delta t\,\sigma_{\mathrm{NESS}}$, a positive number. Nothing forces the divergence to zero. The inequality itself is true, is formalized as an instance obligation, and does real work --- combined with Gibbs' inequality it gives $\sigma \ge 0$ --- but the conclusion drawn from it was not licensed, and it was that conclusion which supplied this framework's entire connection to predictive processing.

\paragraph{The replacement is a published theorem.}
Still, Sivak, Bell and Crooks \cite{still2012} prove that for a system driven by an environmental signal the work dissipated over one step of the drive obeys
\begin{equation}
W_{\mathrm{diss}} \;\ge\; k_B T \left[\, I(X_t ; S_t) \;-\; I(X_t ; S_{t+1}) \,\right].
\end{equation}
The bracket is the \textit{nonpredictive} information: what the structure retains about the signal's present that says nothing about the signal's future. Dissipation is bounded below by it. Memory that fails to predict is not merely useless to the structure; it is thermodynamic waste, and the waste is quantified in energy.

\paragraph{Three consequences, and they are theorems rather than readings.}
The bound is carried in Lean as a class field, \texttt{PredictiveDissipation.\allowbreak still\_bound} (\texttt{Phase3\_PredictiveThermodynamics}), for the same reason every other physical postulate here is: its \texttt{axiom} form, quantified over all joint laws with the dissipated work fixed, is refutable, and \texttt{Examples}~\S18.4 refutes it. What is \textit{derived} is everything downstream.

First, \textit{the bracket is non-negative}, and this is proved rather than assumed. If the structure does not act on its environment, then $X_t \to S_t \to S_{t+1}$ is a Markov chain and the data processing inequality gives $I(X_t ; S_{t+1}) \le I(X_t ; S_t)$: a system cannot know more about the signal's future than about its present (\texttt{predictiveInfo\_\allowbreak le\_\allowbreak mutualInfo}). Mathlib carries the Kullback--Leibler divergence but no mutual information, so the latter is defined here as the divergence of the joint law from the product of its marginals, which is the standard definition and makes the library's data processing lemma apply directly.

Second, \textit{the second law follows} instead of being assumed. Dissipation is bounded below by a quantity now known to be non-negative, so $W_{\mathrm{diss}} \ge 0$ (\texttt{dissipatedWork\_\allowbreak nonneg}). This mirrors the role Gibbs' inequality plays for the older bound, with one difference that matters: Gibbs' inequality compares a system to an arbitrary reference distribution, whereas the data processing inequality compares the system to its own future, which is a quantity selection can act on.

Third, and this is the point of the replacement, \textit{the inference now runs in the valid direction}. The quantity of interest sits on the small side of the inequality: $I(X_t;S_t) - I(X_t;S_{t+1}) \le W_{\mathrm{diss}}/k_B T$. No limit is required. Any bound on dissipation is immediately a bound on wasted memory, so a structure held to an energy budget is thereby held to a prediction standard: this is \texttt{nonpredictive\_\allowbreak le\_\allowbreak dissipation}. In the idealised quasi-static case $W_{\mathrm{diss}} = 0$, the theorem \texttt{predictive\_\allowbreak eq\_\allowbreak of\_\allowbreak no\_\allowbreak dissipation} gives that every retained bit is predictive. This is a weaker statement than the one it replaces, and it is the one that is true.

\paragraph{The hypothesis is load-bearing, and we exhibit what breaks without it.}
The data processing step requires that the structure not write to its environment. In the formalization this is not an assumption stated beside the theorem but the shape of the evolution itself: the signal's step is applied to the pair as the parallel kernel $\mathrm{id} \parallel \kappa$, which acts as the identity on the system coordinate and through $\kappa$ on the signal coordinate, so no channel from $X_t$ to $S_{t+1}$ exists. \texttt{Examples}~\S18.3 shows the restriction is not decoration: a Markov kernel on the \textit{pair} which copies the system's state into the signal carries an independent two-bit law to a perfectly correlated one, raising mutual information from zero to a positive value. An agent that steers its environment can indeed know more about that environment's future than about its present, and Still's bound is not about such an agent.

\paragraph{Both regimes are exhibited, not just described.}
\texttt{Examples}~\S18 builds two systems over one correlated two-bit joint law, differing only in the signal's dynamics. When the signal is frozen, every remembered bit is a predictive bit, the nonpredictive information is zero, and the instance is permitted to dissipate nothing --- with the memory itself proved strictly positive, so the conclusion is about a system that does remember something. When the signal is redrawn from its stationary law at each step, the predictive information is exactly zero, the \textit{entire} memory is nonpredictive, and the instance is \textit{forced} to dissipate $k_B T$ times its whole mutual information. Establishing that the mutual information of the witness is neither zero nor infinite is where the work of that section lies.

\paragraph{An equality, where the old argument had only a bound.}
Kawai, Parrondo and Van den Broeck \cite{kawai2007} show that the average dissipated work is exactly $k_B T$ times the relative entropy between the forward and time-reversed phase-space densities. Carried here as a predicate on an instance rather than as a further postulate, it replaces the abstract dissipation field with a quantity an experiment can estimate --- the statistical distinguishability of a recording of the system from the same recording run backwards --- and yields the statement that the irreversibility of the dynamics is at least the amount of memory that fails to earn its keep (\texttt{nonpredictive\_\allowbreak le\_\allowbreak arrow}).

\paragraph{What this does not establish.}
It does not deliver the Free Energy Principle. Still's bound says that prediction is thermodynamically favoured; it says nothing about hierarchical generative models, variational free energy, or Bayesian inference, and we do not claim to have derived any of them. The Free Energy Principle \cite{friston2010, friston2009, hohwy2013, clark2016, spisak2026} and the General Resonance Theory of Consciousness \cite{hunt2019} are consonant with what is proved here, which is a weaker relation than derivation and the only one we assert. Nor does it solve the symbol grounding problem \cite{harnad1990}. Maximal predictive information is compatible with the structure storing a lossy, compressed or otherwise unrecognisable function of the signal; a state that predicts is not thereby a state that means, and earlier versions of this section overstated the case. What survives of that argument is narrower and still worth saying: whatever internal states a physical structure holds, those that do not help predict its environment are paid for in heat, so the pressure toward states indexed to the environment is energetic rather than semantic. We continue to call the phenomenon \textit{structural resonance}, and there is no teleological drive to ``survive'' in it: predicting the environment is simply the cheaper of the two ways to hold a memory.

\section{Macroscopic Scaling: A Theorem and a Commitment}
\label{sec:scaling}
This is the chain's fourth step, and it is deliberately not numbered as a derivation. The change is not cosmetic. Earlier drafts ran two steps of different kinds together in a single paragraph: a coarse-graining argument, which is a piece of mathematics, and the identification of the coarse-grained medium with the brain's endogenous electromagnetic field, which is an empirical claim about cortex. Presenting the second as though it inherited the licence of the first made it look like a deduction, and it is not one. Separated, the empirical claim becomes the framework's central commitment and the place where it is most easily wrong --- which is where a claim of this kind belongs.

\subsection*{The formal half: coarse-graining is a theorem, and a narrow one}
A network of discretely coupled units has a coupling energy that is a symmetric double sum over pairs. \texttt{Phase2\_MeshConvergence.lean} proves that this sum converges to a continuous integral: along a sequence of regular triangulations of a region $S$ whose edge regions shrink to points, the discrete energy $\tfrac{1}{2}\sum_u\sum_v \mu(R_{uv})\, f(x_u)$ converges to $\int_S f\,d\mu$ (\texttt{mesh\_\allowbreak refinement\_\allowbreak convergence}), witnessed on the uniform partition of $[0,1)$ in \texttt{Examples}~\S6. The identification that makes this a coarse-graining rather than an arbitrary Riemann sum is \texttt{sum\_sum\_mul\_of\_symm}: because the summand is symmetric, the double sum is exactly the midpoint rule over unordered edges, so the discrete energy is a quadrature of the continuous one rather than a different quantity that happens to converge to it.

What this establishes is worth stating exactly, because it is less than the phrase ``coarse-graining, governed by the Renormalization Group'' suggests. It is a statement about an \textit{energy functional}, not about a dynamics: nothing here shows that trajectories of the discrete Kuramoto system converge to solutions of a field equation, and the earlier appeal to the Renormalization Group was decorative --- no renormalization group flow is constructed anywhere in this development. The limit object is a scalar density on the substrate, in the intended reading the magnitude of the stress-energy tensor, and it is not an electromagnetic field. The continuous object the later sections use, \texttt{ContinuousNeuralField}, is \textit{posited} as a structure with a coupling kernel $K$; no theorem produces it from the discrete system. The gap between the finite Kuramoto model and its continuum limit is the propagation-of-chaos problem, which we have not addressed and record as open.

\subsection*{The empirical half: which physical field realizes the kernel}
The formal half says that a large network of symmetrically coupled units admits a continuum description with a symmetric kernel. It says nothing about which physical field in cortex that kernel describes. Our answer is an empirical commitment, and we state it as one:

\begin{quote}
\textit{In the mammalian cortex, the field realizing the coarse-grained coupling kernel is the endogenous electromagnetic field.}
\end{quote}

This is a claim about biology, not a consequence of anything above it. It is what makes the framework falsifiable and it is what we expect to be attacked.

\paragraph{What supports it.} Neurons do not only signal through synapses: transmembrane currents generate extracellular fields that shift the membrane potentials of neighbouring cells, a mechanism known as ephaptic coupling. Endogenous fields of $1$--$5$ mV/mm have been shown \textit{in vitro} to entrain spike timing \cite{frohlich2010, anastassiou2015}; realistic head models put mesoscopic ephaptic effects within the range where they can bias network dynamics \cite{ruffini2020modeling}; cable-theoretic treatments show field coupling is sufficient for coincidence detection in the auditory brainstem \cite{goldwyn2016neuronal}; and stable electric fields have been argued to carry information that survives representational drift in the underlying spiking population \cite{pinotsis2022, pinotsis2023ephaptic, pinotsis2023cytoelectric, cunha2024ephapticity}. The field is spatially continuous and superposes contributions from large populations, which is the property the framework needs: it is a medium in which spatially separated activity is combined without a further mechanism for combining it. This is the integrative role the CEMI field theory assigns it \cite{mcfadden2002, mcfadden2020, pockett2012}.

\paragraph{What the evidence does not support, and a claim we withdraw.} Earlier drafts asserted that \textit{the continuous macroscopic EM field is the primary dissipative structure interacting with the universe}. The word ``primary'' is not supported by any of the work cited above and we withdraw it. What the experiments show is \textit{modulation}: Anastassiou and Koch report that endogenous fields of $1$--$5$ mV/mm shift spike timing by one to three milliseconds \cite{anastassiou2015}, an effect that is real, reproducible, and subthreshold. It biases when a cell fires; it does not decide whether it fires. The corrected claim is that the endogenous field is \textit{modulatory in its effect on individual neurons and integrative in its spatial reach} --- weak per synapse, but the only variable in the system that is defined everywhere at once. The framework needs the second property, not the first, and conflating them was the error.

\paragraph{Its critics.} The objection with the longest history is that the fields are too weak to matter, and it has been pressed from within the field-theoretic camp: Pockett, who has herself defended an electromagnetic theory of consciousness \cite{pockett2012}, catalogues its difficulties \cite{pockett2002} --- the measurement problem of relating gross field structure to reported experience, the absence of a constant relationship between the two, and the question of whether the field does causal work or merely reflects it. The quantitative case for scepticism has strengthened since. V\"or\"oslakos and colleagues measured intracranial fields during transcranial stimulation in rats and human cadavers and found that the great majority of applied current is shunted through the scalp, so that conventional protocols deliver fields at or below the $1$ V/m range in cortex \cite{voroslakos2018}. That is a bound on \textit{applied} fields rather than endogenous ones, but it constrains how much causal work a field of that magnitude can be doing, and it is the reason we do not claim the field drives spiking.

\paragraph{What would falsify it.} We state the failure conditions rather than leaving them to a referee.
\begin{enumerate}
\item \textit{Epiphenomenality.} If the endogenous field can be cancelled or shunted --- by a conductive medium that nulls the extracellular potential without altering transmembrane currents --- and neither behaviour nor report changes, the identification fails. This is the decisive experiment and it is, in principle, available.
\item \textit{A magnitude gap.} If the spatial integration the framework requires demands field effects an order of magnitude above the $1$--$5$ mV/mm measured in cortex, the commitment fails on arithmetic. The relevant number is not whether ephaptic coupling exists but whether its coupling strength suffices to place the system above the critical coupling $K_c$ of Derivation~7.
\item \textit{A timescale mismatch in the wrong direction.} The prediction of Section~\ref{sec:prediction} turns on the field's dynamics being fast relative to the astrocytic and clearance variables. If recovery from sleep inertia tracks an exponential set by the electrical timescale rather than the delayed sigmoid the slow variable predicts, the framework loses the one prediction that is its own.
\item \textit{Dissociation of field coherence from reported unity.} If large-scale field coherence is preserved across states in which experiential unity is lost --- or absent in states where it is retained --- the identification of the global section with unified experience has no physical carrier.
\end{enumerate}
A framework that survives all four is not thereby true. It is merely still standing, which is more than most theories of consciousness arrange to be able to lose.

\subsection*{Geometric frustration, and the regime our theorems actually cover}
Earlier drafts grounded the system's memory capacity in \textit{geometric frustration}: a highly coupled network cannot bring all its nodes to their lowest energy simultaneously, producing a jagged landscape of near-degenerate states with the capacity to store many patterns. That argument is standard and it is not what this development formalizes. Confronting the discrepancy is the honest course, so we do it here rather than in a footnote.

Frustration requires competing couplings --- in the Kuramoto setting, a sign structure that no phase assignment can satisfy on every edge at once. The formal setting admits this: \texttt{KuramotoSystem} carries $A : V \to V \to \mathbb{R}$, symmetric and otherwise unconstrained, so signed couplings are representable. But every theorem in this development that says anything about \textit{where} the dynamics ends requires strictly positive coupling. \texttt{phase\_\allowbreak locked\_\allowbreak minimizes\_\allowbreak potential} assumes $A_{ij} > 0$; \texttt{potential\_min\_iff\_phase\_locked} inherits it; the class field \texttt{ThermodynamicCover.A\_pos} demands it of every instance, and hence of every conclusion of Derivation~5; and the convergence theorem of \texttt{Phase4\_\allowbreak RotatingFrame}~\S7 requires the strictly stronger uniform bound $0 < a \le A_{ij}$. A network with all-positive couplings is an unfrustrated ferromagnet whose potential has one minimum up to a global phase shift. It is the opposite of a jagged landscape, and it has no capacity to store multiple patterns at all. \textbf{The system in which memory is grounded and the system our theorems describe are not the same system.}

The division is not arbitrary, and it is worth recording where it falls. What survives with no assumption on the sign of the coupling is the dynamical half: well-posedness on all of $\mathbb{R}$ (\texttt{is\_kuramoto\_trajectory\_exists}, \texttt{\_unique}), covariance under passage to a rotating frame, monotone descent of the potential (\texttt{dynamic\_potential\_antitone}), finiteness of the dissipation integral, and --- through Barbalat's lemma --- that the motion stops along every trajectory (\texttt{velocity\_sq\_tendsto\_zero}). \textit{That} the motion stops is frustration-agnostic. \textit{Where} it stops is not, and every claim this framework makes about the destination lives on the positive side of that line. On the continuum side the accounting differs slightly: \texttt{phase\_locked\_minimizes\_entropy\_of\_symm} assumes only that the kernel is symmetric and so tolerates signed kernels, but it is conditional on a dynamically phase-locked field existing --- which is precisely what frustration may prevent.

We therefore scope the claim rather than repeat it. The memory capacity of a frustrated cortical network is not supported by any theorem here, and we do not assert it as part of the chain. What the chain uses is the unfrustrated regime, in which synchronization is reached and the global section exists. Whether a frustrated network can hold many near-degenerate patterns \textit{and} still synchronize enough of itself to admit a global section is an open question about mixed-sign oscillator networks, and it is the most substantive gap we know of in this framework. Recording it as such costs a claim we would have liked to make and is preferable to holding two incompatible pictures at once.

\subsection*{The supporting physiology}
The continuous medium is physically supported by astrocytic syncytia---non-neuronal networks joined by gap junctions that provide macroscopic cytoplasmic continuity for energy and ion flows \cite{verkhratsky2018}. Managing the thermodynamic cost of this state requires sophisticated micro-bioenergetics. Dynamic mitochondrial networks function as deep-level Mitochondrial Information Processing Systems (MIPS), transducing biological and environmental information to sustain the coherence of the macroscopic field \cite{picard2022}. Discrete synaptic firings, coupled with mitochondrial micro-mechanics, generate and sustain the field. Because the external environment is continuous, achieving maximal thermodynamic structural resonance is optimally realized within this continuous macroscopic medium \cite{vanbree2025}.

\section{Derivation 5: Global Sections and the Unity of Consciousness}
The system is now characterized by a continuous macroscopic electrodynamic field generated by millions of coupled micro-components, forced to resonate with the environment. Following Section~\ref{sec:scaling}, the coupling regime assumed from here on is the unfrustrated one --- all couplings positive --- which is what every theorem below requires and what the frustrated network of the memory argument is not. 

The Kuramoto model \cite{kuramoto1984} of coupled non-linear oscillators guarantees that, past a critical coupling threshold, these distributed local fields undergo a phase transition into unified synchronization \cite{bauer2022}. Our Lean proofs (\texttt{Phase4\_KuramotoDynamics.lean}) confirm this phase-locked state minimizes the Lyapunov potential of the system, in the precise sense discussed in Section~\ref{sec:soundness}: the functional minimized is the coupling potential read in the frame rotating with the natural frequency, which \texttt{Phase4\_RotatingFrame.lean} shows is the honest reduced description of a system of identical oscillators. 

This phase transition mathematically glues local synchronized states into a singular \textit{Global Section} of a probability sheaf (\texttt{Phase5\_GlobalSection.lean}). The EM field acts as a single, indivisible topological space, providing the \textit{Unity of Consciousness}—a necessary condition for integrated experience that solves the binding problem. While Global Workspace Theory \cite{baars1997, dehaene2014} models this unity as a broadcast architecture and Integrated Information Theory \cite{tononi2016} quantifies it via $\Phi$, our derivation provides a continuous physical mechanism. The contrast is worth drawing carefully, because both theories are addressing the same explanandum from different directions.

We state the scope of the Lean result precisely, because it is narrower than the prose above. \texttt{ThermodynamicCover} demands that the cover's phase field minimize the reduced Kuramoto potential, and \texttt{potential\_\allowbreak min\_\allowbreak iff\_\allowbreak phase\_\allowbreak locked} characterizes those minimizers exactly: they are the phase-locked configurations, in both directions. Every instance is therefore already phase-locked (\texttt{ThermodynamicCover.phase\_\allowbreak locked}), and the class's single condition on its local data --- that patches at a common phase agree where they overlap (\texttt{section\_\allowbreak agrees\_\allowbreak of\_\allowbreak phase\_\allowbreak eq}) --- makes the family of local sections compatible. \texttt{probability\_\allowbreak glue\_\allowbreak unique}, which is the sheaf property of the probability presheaf and contains no physics at all, then glues that family into exactly one global section.

An earlier version of this structure proved less than the prose claimed, and we record the correction because it changed what the theorem says rather than how it is stated. \texttt{LocalSectionSynchronization} used to carry a \emph{global} section as a class field, together with a second field declaring every local section to be a restriction of it. The global object therefore existed before the sheaf condition was ever consulted, and \texttt{global\_\allowbreak section\_\allowbreak from\_\allowbreak thermodynamics} could do no more than rule out competitors to an object the instance had supplied. Both fields are gone. Every field of the class now mentions a single patch or a single overlap; the global section is constructed (\texttt{ThermodynamicCover.invariant\-Measure}); and the removed field survives as a \emph{theorem} about that construction (\texttt{ThermodynamicCover.sync\_\allowbreak to\_\allowbreak section\_\allowbreak eq}), so the local sections are restrictions of one common measure because the sheaf condition says so, not because an instance was required to name one.

\texttt{Examples.lean}~\S14 is the witness that the difference is not notational. Two patches carry two different mass profiles, $(2,1,7)$ and $(5,1,3)$ across the three sites, agreeing only at the site they share, where both put mass $1$ --- which is what discharges the overlap condition, by computing two numbers rather than by construction. The section the theorem returns has profile $(2,1,3)$: it is neither of the two profiles (\texttt{gluedW\_\allowbreak ne\_\allowbreak leftW}, \texttt{gluedW\_\allowbreak ne\_\allowbreak rightW}), and neither of them restricts correctly to both patches (\texttt{leftW\_\allowbreak glues\_\allowbreak nothing}, \texttt{rightW\_\allowbreak glues\_\allowbreak nothing}). The global state carries information that no single patch had. \texttt{Examples.lean}~\S13, built the older way through the constructor \texttt{LocalSectionSynchronization.of\-InvariantMeasure}, is retained alongside it for the negative result it carries: its phase field is not the constant function --- the second patch a full turn ahead of the first --- and by \texttt{ThermodynamicCover.phase\_\allowbreak locked} that is as far from constant as any instance can get, since no instance can carry phases differing by anything a measure is allowed to see.

Two hypotheses carry physical content, and until recently each was an obligation on every instance. One still is: that synchronized patches agree on their overlaps is a modelling assumption, not a consequence of any dynamics we formalize. The other, \texttt{thermodynamic\_\allowbreak equilibrium}, asks that the cover already sit at the potential minimum, and no theorem says an \emph{arbitrary} system's dynamics takes it there. ``A thermodynamic phase transition into unity'' is therefore still doing causal work that no general Lean result supports, though it is no longer entirely without one: \texttt{Examples.lean}~\S15 exhibits a two-oscillator trajectory that starts a quarter turn out of phase and converges to the global minimum of the dynamic potential, with the order parameter tending to $1$. That trajectory is not an isolated construction: \texttt{kuramoto\_\allowbreak cauchy\_\allowbreak problem} proves the Kuramoto initial value problem well posed on all of $\mathbb{R}$, so through every initial state there passes exactly one trajectory, and the exhibited one is the only solution through its own. Nor is its settling down peculiar to it: along every trajectory of a zero-frequency system the dynamic potential converges, and the total dissipation $\int_0^\infty \sum_i \dot\theta_i^2$ is finite and equal to the potential's total drop (\texttt{dynamic\_\allowbreak potential\_\allowbreak tendsto}, \texttt{dissipation\_\allowbreak integral\_\allowbreak tendsto}). The motion also stops: a finite integral does not by itself force its integrand to zero, but the dissipation rate is Lipschitz in time, so Barbalat's lemma applies, and every trajectory satisfies $\sum_i \dot\theta_i^2 \to 0$ (\texttt{velocity\_\allowbreak sq\_\allowbreak tendsto\_\allowbreak zero}; Mathlib has no Barbalat, so it is proved here as \texttt{tendsto\_\allowbreak zero\_\allowbreak of\_\allowbreak lipschitz\_\allowbreak of\_\allowbreak integral\_\allowbreak le}). That last step --- locating where the motion stops --- is now taken, though conditionally, and by a different route than we had recorded. We had marked it as blocked on the LaSalle invariance principle, which Mathlib does not have, and on the absence of a compact invariant set in the state space $V \to \mathbb{R}$. That was the wrong diagnosis. LaSalle is the tool for a system with a decreasing functional and nothing else; this one has more. The flow is a gradient flow of the dynamic potential, so $\dot V = -\sum_i\dot\theta_i^2$ is an identity rather than an inequality; the minimisers of $V$ are characterised exactly; and on the region where every pair of phases lies within a quarter turn a Polyak--\L{}ojasiewicz estimate holds, $2a^2 W \le (\sum_{ij}A_{ij})\sum_i \dot\theta_i^2$, where $W$ is the potential's excess above its minimum and $a>0$ is a lower bound for the coupling (\texttt{lojasiewicz\_\allowbreak estimate}). Its proof is two extremal indices and the identity $\sin^2 D = (1-\cos D)(1+\cos D)$: the site carrying the largest phase is pulled backwards at rate at least $a\sin D$, where $D$ is the spread, while the excess is at most $\tfrac12(\sum_{ij}A_{ij})(1-\cos D)$. The compactness objection dissolves as well, because the invariant region is produced by the Lyapunov function rather than assumed: a single pair contributes $\tfrac12 a(1-\cos)$ to the excess, so an excess below $a/2$ forces every pairwise cosine positive for all time, and continuity on the connected half-line then prevents any pair from changing branch (\texttt{spread\_\allowbreak le\_\allowbreak of\_\allowbreak small}). Descent plus the estimate integrates to $W(t) \le W(0)e^{-ct}$ with $c = 2a^2/\sum_{ij}A_{ij}$ (\texttt{excess\_\allowbreak decay}), the rate makes each $|\dot\theta_i|$ exponentially small and hence integrable on $[0,\infty)$, and a trajectory with integrable velocity converges. The conclusion is \texttt{kuramoto\_\allowbreak tendsto\_\allowbreak global\_\allowbreak minimum}: if the phases start within a quarter turn of each other and the initial excess is below $a/2$, the trajectory converges to a phase-locked configuration which is a \emph{global} minimiser of the dynamic potential, with order parameter $r^2 = 1$. That is the configuration \texttt{thermodynamic\_\allowbreak equilibrium} postulates, reached rather than assumed. \texttt{Examples.lean}~\S17 fires it on three sites with initial data $(0,0,\tfrac12)$, where the Kuramoto system has no closed-form solution: the trajectory comes from the existence theorem and its limit from the convergence theorem, so neither is written down.

\texttt{Phase5\_EquilibriumBridge.lean} closes the gap that remained between the two results. \texttt{ThermodynamicCover.of\-Convergent\-Trajectory} takes a cover whose phase field is the limit of such a trajectory and returns an instance whose \texttt{thermodynamic\_\allowbreak equilibrium} field is discharged \emph{by that convergence}; the cover's coupling matrix is the coupling matrix of the dynamics rather than a separately chosen one, so the system whose equilibrium the class asserts and the system whose trajectory is run are the same system. \texttt{Examples.lean}~\S17.1 is the resulting witness: three patches over the three-site substrate, each carrying its own mass profile on its own open set, above a phase field that no line of the construction writes down. The trajectory that reaches it starts at $(0,0,\tfrac12)$, which is provably not phase-locked (\texttt{trioCover\_\allowbreak start\_\allowbreak not\_\allowbreak locked}), so on this cover the configuration Derivation 5 requires is arrived at rather than posited. The state the three patches glue to has profile $(2,1,3)$ and is none of the three profiles the instance carries (\texttt{trioW\_\allowbreak ne\_\allowbreak glued}), so one cover now carries both the emergence reading of the gluing and the derived reading of the equilibrium. What is still assumed on it is the overlap agreement, and nothing else.

The two hypotheses cannot be dropped, and this is the honest scope of the result. Splay and twisted configurations are equilibria of the same flow, so ``every trajectory reaches the unified state'' is false as a general statement, and the initial-data condition is the standard arc condition under which the true statement holds. What is claimed is therefore conditional --- unity is reached from configurations already sufficiently coherent --- and not that an arbitrary initial state is carried into unity. What Lean establishes about the gluing itself is the other half: \emph{given} locally synchronized patches, a global state exists, is unique, and is produced by the gluing rather than presupposed by it.

Integrated Information Theory begins from phenomenological axioms and derives a scalar $\Phi$ measuring irreducibility of a system's cause-effect structure, with the exclusion postulate selecting a single maximum-$\Phi$ complex as the locus of experience. Our sheaf-theoretic global section plays a structurally analogous role --- it too picks out a distinguished unified object --- but it is arrived at from the opposite end: not postulated from the character of experience, but forced by a thermodynamic phase transition in a physical field. This buys two things IIT does not have. First, the unity condition is checkable from field dynamics rather than requiring the computation of $\Phi$, which is intractable for systems of realistic size and whose definition has undergone repeated revision \cite{tononi2016}. Second, exclusion falls out rather than being postulated: gluing either succeeds on a given cover or it does not, so there is no separate principle needed to rule out overlapping conscious subsystems. It also inherits IIT's exposures. Critics have argued that $\Phi$ can be high in systems with no plausible claim to experience, such as large feedforward-free grids \cite{aaronson2014}; our criterion is vulnerable to the mirror objection, since a physical system could in principle satisfy the gluing condition without being cognitively interesting. We take this seriously rather than dismissing it: our reply is that gluing is necessary but not sufficient, and that the topological boundary of Derivation 1 and the reflexive structure of Derivation 6 are additional required conditions. IIT, by contrast, treats $\Phi$ as both necessary and sufficient.

Global Workspace Theory faces a complementary difficulty. It specifies a functional architecture --- a limited-capacity workspace broadcasting to specialized modules --- without specifying what physical property of broadcasting makes the broadcast content conscious, leaving the theory open to the charge that it explains access consciousness while leaving phenomenal consciousness untouched \cite{block1995}. Our framework can be read as supplying the missing physical substrate for the workspace: the continuous macroscopic field \emph{is} the medium in which integration is realized, and the $K_c = 2D$ threshold gives a physical condition for ignition rather than a functional stipulation of it. Where GWT is agnostic about implementation, we make a falsifiable commitment --- unity requires a continuous physical medium, which predicts that a functionally equivalent discrete broadcast architecture would not be conscious. We regard this as a genuine cost of the position, not a free advantage: it is the point at which our framework departs most sharply from computational functionalism and where it is most readily falsified. Contrast this derivation with panpsychism: consciousness is not assumed as a fundamental property of matter. It requires the initial existence of a topological defect to establish a bounded ``inside'' (Derivation 1), followed by a thermodynamic phase transition into unity that occurs when integrated spatial coupling exceeds the thermodynamic noise threshold ($K_c = 2D$). Without both the foundational topological boundary and the subsequent mathematically defined phase transition, the system remains a fragmented, non-conscious collection of micro-states. A unified field of perception, however, is not yet a ``Self.''

\section{Derivation 6: Reflexive Topology and the Emergence of the Self}
While a global section provides Unity, a ``Self'' requires reflexivity. The unified EM field is a dissipative structure that must resonate with external perturbations while managing its own internal phase transitions. Time evolution naturally drives the field to minimize thermodynamic friction with its own internal state-changes. 

This mandates \textit{auto-resonance}: the field must predict itself. The ``Self'' emerges as a higher-order topological feature when the internal geometry folds to include a low-dimensional avatar of its own computational and causal boundary within its predictive model (\texttt{Phase6\_ReflexiveTopology.lean}) \cite{levin2019}. The system is unified, predictive, and explicitly maps its own distinct existence from the environment. This architecture provides the physical foundation for a self-aware consciousness, bounded by the continuous measure thermodynamics of Landauer erasure (\texttt{Phase3\_MeasureThermodynamics.lean}). We are explicit about what carries the weight here. \texttt{reflexive\_topology\_implies\_self} is the Banach fixed-point theorem with the physical commitments in its hypotheses: the space of global sections is assumed nonempty, metric and complete, and the predictive map is assumed to contract by $1/2$. \texttt{Examples.lean}~\S10 discharges all four on the three-site substrate of Derivation 5, and the metric is no longer a placeholder. The section first pierces the sheafification: on a discrete substrate every point has a smallest open neighbourhood, so the mass a germ carries at a point is well defined, and \texttt{massEquivOn} proves that over \emph{any} open the sections of the probability sheaf \emph{are} the finite measures on that open, read as densities against counting measure --- the global case being $U = \top$. Mathlib supplies no inverse to this sheafification --- its \texttt{TopCat} sheafification has neither an adjunction nor an \texttt{isIso\_toSheafify} --- so the correspondence had to be constructed. The metric on sections is then the uniform distance between the densities of the measures they glue to --- equivalent to their total variation on a finite substrate, though not equal to it --- and it is proved complete by exhibiting the limit section of a Cauchy sequence of densities, not by the degenerate route of showing Cauchy sequences are eventually constant. The self-prediction map is the relaxation ``average the current field with the baseline,'' and we prove it contracts distances by \emph{exactly} one half, that it is not constant, and that its fixed point is unique. An earlier version of this witness used the $0/1$ metric, under which every contraction is constant; that theorem is kept in the source as the record of why the earlier witness was empty. A separate gap, closed at the same time, concerned the avatar rather than the fixed point. \texttt{Reflexive\-Boundary} carries a field \texttt{auto\_\allowbreak resonance} sending the global state to a section over the avatar region, and nothing constrained it: a constant function was a legal avatar, and \texttt{reflexive\_\allowbreak topology\_\allowbreak implies\_\allowbreak self} consumes only the predictive model, so no theorem in the development mentioned the field the avatar is supposed to be reading. The constraint is now stated as a predicate rather than added to the structure --- \texttt{Is\-Restriction\-Resonance}, that the avatar's state is the presheaf restriction of the global state --- and two theorems say why it is the right one. In the positive direction, if a family of resonant avatars \emph{covers} the substrate then the global state is determined by their local readings alone (\texttt{eq\_\allowbreak of\_\allowbreak avatar\_\allowbreak eq}): unity is not assumed alongside the avatars but recovered from them, by the same sheaf condition that Derivation 5 uses. In the negative direction, replacing any avatar by a constant leaves a legal \texttt{Reflexive\-Boundary} with the same region, the same predictive model and the same Self (\texttt{self\_\allowbreak of\_\allowbreak const\-Resonance}), so the fixed-point theorem cannot see the difference and the predicate is doing the work rather than restating it. Both halves are witnessed on the three-site substrate, where the avatar region provably separates two states of the field, so the condition is a constraint there and not a formality. What is still not established is that any field dynamics produces this particular map or performs the restriction, or that the contraction constant $1/2$ is read off an entropy production rate rather than built into the map's definition. We also note a negative result found in the course of this work: the Lévy--Prokhorov metric, which the natural weak-convergence reading would suggest, does \emph{not} support this argument on a discrete substrate, because the $\varepsilon$-thickening of a set is the set itself for $\varepsilon < 1$ and the relaxation map is consequently not a Lévy--Prokhorov contraction.

\section{Derivation 7: Continuous Stochastic Thermodynamics and Field Theory}
To move beyond finite graph analogies, we model the macroscopic electrodynamic field as a \textit{Continuous Neural Field}. True ephaptic coupling is a continuous spatial phenomenon, not a discrete network. We extend the discrete Kuramoto model to a continuous spatial coupling kernel $K(x,y)$ (\texttt{Phase8\_ContinuousField.lean}).

Subject to thermodynamic noise $D$, the system undergoes non-equilibrium stochastic thermodynamics. Rather than a static Landauer bound, the field dynamics minimize an entropy production functional $\sigma$:
\begin{equation}
\sigma = \int \frac{1}{D} \left( \omega(x) + \int K_t(x,y) \sin(\theta_y - \theta_x) dy \right)^2 dx
\end{equation}
Biological fields dynamically deform their spatial topology $K_t(x,y)$ to undergo gradient descent on this entropy production functional.

That gradient descent is a theorem rather than a modelling assertion, for finite substrates. Reading $\sigma$ as a functional of the coupling kernel at fixed phases, \texttt{hasFDerivAt\_sigmaOfKernel} computes its Fr\'echet derivative in closed form,
\begin{equation}
\frac{\partial \sigma}{\partial K(a,b)} = \frac{2}{D}\Big(\omega(a) + \sum_y K(a,y)\sin(\theta_y - \theta_a)\Big)\sin(\theta_b - \theta_a),
\end{equation}
and \texttt{sigmaOfKernel\_eq\_entropy\_production\_rate} identifies the functional being differentiated with \texttt{entropy\_production\_rate} itself whenever the substrate carries counting measure. \texttt{structural\_resonance\_decreases\_entropy\_production} then concludes that $\sigma$ is non-increasing along any coupling trajectory relaxing down its own gradient at a positive rate $c$. Earlier drafts proved this only for an abstract functional $S$ on an abstract inner-product space with the gradient supplied as data; the identification of that $S$ with $\sigma$ was informal, and Table 1 flagged the row accordingly. Two limitations stay explicit, and the first is narrower than we previously reported. The derivation is for \emph{finite} substrates. We had recorded the obstacle to the continuum case as differentiation under the integral sign with respect to the kernel; that was a misdiagnosis. $\sigma$ is not a general functional that happens to be written as an integral --- it is a quadratic functional of an \emph{affine} image of the kernel, since the drift is a fixed $\omega$ plus a linear operator applied to $K$, and $\sigma$ is a constant times the squared norm of the drift. A squared norm is Fr\'echet differentiable on any real inner-product space and an affine map is its own derivative, so the composite differentiates by the chain rule with no limiting argument. \texttt{hasFDerivAt\_quadratic\_of\_affine} states that in full generality, and \texttt{hasFDerivAt\_sigmaOfKernel\_of\_operator} re-derives the finite gradient from it, showing that the closed form displayed above is a convenience rather than the content. What the continuum case still needs is therefore one object: the drift map $K \mapsto \left(x \mapsto \int K(x,y)\sin(\theta_y-\theta_x)\,dy\right)$ as a bounded operator $L^2(\mu\otimes\mu) \to L^2(\mu)$, which is a Cauchy--Schwarz estimate --- the integral kernel is bounded by $1$, so on a finite measure space the operator norm is at most $\mu(M)^{1/2}$. We have not built it, because it requires constructing a continuous linear map between $L^p$ spaces and we have not done that bookkeeping; we record the corrected scope because ``construct one bounded operator'' and ``differentiate under the integral sign'' are different sizes of task. The second limitation is unchanged: the phase field is held fixed throughout, so this is plasticity of the coupling at frozen phases, not joint $(\theta, K)$ dynamics. \texttt{Examples.lean} \S5 exercises the whole chain on a two-site substrate whose coupling relaxes as $e^{-t}$ and whose entropy production decays as $e^{-2t}$, confirming that the gradient is not identically zero and that the descent is strict rather than vacuously monotone.

A second result about $\sigma$ concerns where the descent is headed, and it has been strengthened since the last draft in a way that also corrects an estimate we had recorded. That the phase-locked field minimizes $\sigma$ was proved only against competitors sharing its spatial mean drift --- a restriction that left the theorem close to Jensen's inequality and no more --- and we had judged that lifting it would require modelling how the mean drift varies with the competitor. That judgement was wrong. For a \emph{reciprocal} kernel, one with $K(x,y) = K(y,x)$, the restriction is not a restriction: the coupling integrand $K(x,y)\sin(\theta_y - \theta_x)$ is antisymmetric under exchanging its two sites, because $K$ is symmetric while $\sin$ is odd, so Fubini identifies its double integral with its own negative and the total coupling contribution vanishes. The total drift $\int(\omega + \text{coupling})$ is therefore $\int\omega$ whatever the phases are (\texttt{total\_\allowbreak drift\_\allowbreak eq\_\allowbreak of\_\allowbreak symm}): coupling moves drift between sites but neither creates nor destroys it. Evaluating that conserved quantity at the phase-locked configuration, where the drift is constant, pins its value, and the mean-matching hypothesis follows for every competitor. \texttt{phase\_\allowbreak locked\_\allowbreak minimizes\_\allowbreak entropy\_\allowbreak of\_\allowbreak symm} is the unrestricted statement. Reciprocity is the physically intended condition --- an ephaptic field acts on both sites of a pair alike, and it is the same symmetry \texttt{ThermodynamicCover.A\_symm} already imposes in Derivation 5 --- but it is not a formality, and we prove that rather than assert it. \texttt{Examples.lean}~\S16 exhibits a \emph{one-way} coupling on two sites, in which $a$ feels $b$ and $b$ does not feel $a$, whose phase-locked field is strictly beaten: displacing the second phase to $-\pi/2$ cancels the first site's natural drift while adding none at the second, halving $\sigma$. So ``phase-locking minimizes entropy production'' is false for general kernels and true for reciprocal ones, and the hypothesis cannot be removed by working harder.

We define the critical coupling threshold $K_c = 2D$. The phase transition to the unified ``Global Section'' occurs when the mean-field spatial coupling exceeds this thermodynamic noise threshold, linking the emergence of Unity to physical temperature and noise.

Earlier drafts left $K_c = 2D$ as a bare stipulation validated only by simulation. \texttt{Phase8\_SelfConsistency.lean} now proves the half of it that is ordinary real analysis. For the noisy mean-field Kuramoto model with identical frequencies, the stationary density at order parameter $r$ is the von Mises density $\rho(\theta) \propto e^{(Kr/D)\cos\theta}$, and self-consistency requires $r = R(K,r) := I_1(Kr/D)/I_0(Kr/D)$. Mathlib has no modified Bessel functions, so we formalize that ratio directly as a ratio of integrals over $[-\pi, \pi]$. Integration by parts with $u = \sin$, $v = -e^{a\cos}$ gives an exact identity with no boundary term, $\int \cos\theta\, e^{a\cos\theta}\,d\theta = a\int \sin^2\!\theta\, e^{a\cos\theta}\,d\theta$, so $R$ is $a$ times the mean of $\sin^2$ under the von Mises weight. Bounding that mean by $1$ would put the threshold at $D$; the true threshold is $2D$, and the entire factor of two is the sharper bound $\mathbb{E}_a[\sin^2\theta] \le 1/2$, equivalently $I_2(a) \ge 0$. We prove it elementarily, by folding $[-\pi,\pi]$ onto $[0,\pi/4]$ in two reflections ($\theta \mapsto \pi - \theta$, then $\theta \mapsto \pi/2 - \theta$), after which the integrand is a product of $\cos 2\theta \ge 0$ with $2\cosh(a\cos\theta) - 2\cosh(a\sin\theta) \ge 0$ --- the second factor because $0 \le \sin\theta \le \cos\theta$ there and $\cosh$ increases in $|\cdot|$. The conclusion, \texttt{subcritical\_fixed\_point\_eq\_zero}$'$, is that for $K < K_c$ the incoherent state $r = 0$ is the \emph{only} non-negative solution of the self-consistency equation; $r = 0$ is exhibited as a solution, so the uniqueness claim is not vacuous. Three further facts about the threshold have since been proved, and they narrow the gap between this row and a full ``Theorem'' to a single named statement. First, the fold argument gives a \emph{strict} inequality for $a > 0$ (\texttt{vonMisesC2\_\allowbreak pos}, hence $R(a) < a/2$), which settles the threshold itself: at $K = K_c$ the self-consistency map is tangent to the diagonal at the origin but falls strictly below it thereafter, so $r = 0$ remains the only solution and the bifurcation happens strictly \emph{after} $K_c$, not at it (\texttt{fixed\_\allowbreak point\_\allowbreak eq\_\allowbreak zero\_\allowbreak of\_\allowbreak le\_\allowbreak critical}). Second, factoring $R(a) = a\,E(a)$ shows that a non-zero $r$ solves the equation exactly when $E(Kr/D) = D/K$ (\texttt{coherent\_\allowbreak iff\_\allowbreak sRatio\_\allowbreak eq}), so the coherent branch is a level set of the single function $E(a) = \mathbb{E}_a[\sin^2\theta]$. Third, and using only $E < 1/2$ together with compactness, every coherent solution tends to zero as $K \downarrow K_c$ (\texttt{coherent\_\allowbreak branch\_\allowbreak continuous\_\allowbreak at\_\allowbreak threshold}): the branch emerges continuously from the incoherent state rather than appearing at a finite distance from it, which is what makes the transition supercritical in the technical sense. That statement quantifies over \emph{every} solution, so it does not presuppose the uniqueness it does not have. The level-set form also says exactly what uniqueness is: the injectivity of $E$ on $(0,\infty)$. That is now proved, in the stronger form that $E$ is strictly decreasing on $[0,\infty)$ (\texttt{vonMises\-SRatio\_\allowbreak strictAntiOn}), and by an argument that avoids the route we had expected to need. Differentiating the exponential family in its natural parameter gives $E'(a) = -\mathrm{Cov}_a(\cos^2\theta,\cos\theta)$, which costs differentiation under the integral sign and leaves a covariance that is not sign-definite pointwise: symmetrising with an independent copy gives $\tfrac12\mathbb{E}[(X-X')^2(X+X')]$ with $X = \cos\theta$, an integrand changing sign with $X + X'$. Nothing in the proof differentiates anything. The target is kept as the two-point inequality $C_2(a)Z(b) < C_2(b)Z(a)$ for $a < b$, and two moves settle it. \emph{Fold first}: the two reflections that proved $I_2 \ge 0$ carry both integrals onto $[0,\pi/2]$, replacing $e^{a\cos\theta}$ by $W(a,\theta) = 2\cosh(a\cos\theta)$ and confining $\cos\theta$ to $[0,1]$, after which $\cos 2x - \cos 2y$ and $W(b,x)W(a,y) - W(a,x)W(b,y)$ have the same sign, both being governed by $\cos x$ against $\cos y$. This is the symmetrisation $X \mapsto -X$ that the covariance form needed the tilt to perform, done once at the start; its analytic content is one identity, $2\cosh X\cosh Y = \cosh(X{+}Y)+\cosh(X{-}Y)$ with $\cosh$ increasing in $|\cdot|$. \emph{Then cross}: instead of integrating that inequality over a square, let $\alpha$ be the mean of $\cos 2x$ under $W(a,\cdot)$ and $x_0 \in [0,\pi/2]$ a point where $\cos 2x_0 = \alpha$, supplied by the intermediate value theorem; evaluating at $y = x_0$ and integrating in $x$ alone gives the result, since the term carrying $W(b,x_0)$ vanishes by the choice of $\alpha$. No product measure and no Fubini appear. Run with $\cos\theta$ in place of $\sin^2\!\theta$ the same argument needs no fold --- $R(a) = \mathbb{E}_a[\cos\theta]$ is the mean of the variable the family is tilted by --- and gives \texttt{besselRatio\_\allowbreak strictMono}: $R$ is strictly increasing on all of $\mathbb{R}$, the monotonicity of the Bessel ratio our earlier drafts recorded as unproved.

The converse half is \texttt{supercritical\_fixed\_point\_exists}: for $K > K_c$ there is an $r$ with $0 < r \le 1$ solving $r = R(K,r)$. We had expected this to require a second-order expansion of the Bessel ratio, and it does not. Writing $R(a) = a\,E(a)$ with $E(a) = \mathbb{E}_a[\sin^2\theta]$ --- the same factorization the integration by parts already supplies --- the only facts needed are that $E$ is continuous and that $E(0) = 1/2$, the latter because the weight is constant at $a = 0$. Since $K > 2D$ makes $D/K$ strictly less than $1/2$, continuity gives $E > D/K$ on a neighbourhood of the origin, so the map overshoots, $R(K,r) > r$, for all small $r > 0$; at the other end $R \le 1$ always, because $R$ is a mean of $\cos\theta$, so the map undershoots at $r = 1$. The intermediate value theorem closes the gap. Continuity of $E$ is Mathlib's continuity of a parametric interval integral with jointly continuous integrand, so no dominated-convergence argument is written by hand, and the existence proof never uses monotonicity of $R$: the fixed point is produced by the intermediate value theorem, not by iterating the map. That $R$ is in fact strictly increasing is proved below, and no threshold result depends on it. \texttt{critical\_coupling\_is\_threshold} packages the two halves, and \texttt{exhibits\_phase\_transition\_coherent} applies the supercritical half to any substrate satisfying the predicate of the previous paragraph --- so exceeding $K_c$ now implies the existence of a coherent stationary order parameter rather than merely comparing against a named number. With uniqueness available both are sharpened --- \texttt{supercritical\_\allowbreak fixed\_\allowbreak point\_\allowbreak existsUnique}, \texttt{supercritical\_\allowbreak solution\_\allowbreak set} (the non-negative solutions are exactly $\{0\}$ below threshold and $\{0,r\}$ above it) and \texttt{exhibits\_\allowbreak phase\_\allowbreak transition\_\allowbreak unique\_\allowbreak coherent} --- and the two monotonicities together fix the shape of the branch: for $K_1 < K_2$, $E(a_i) = D/K_i$ with $E$ decreasing puts the concentrations in the order $a_1 < a_2$, and $r_i = R(a_i)$ with $R$ increasing carries that to $r_1 < r_2$ (\texttt{coherent\_\allowbreak branch\_\allowbreak strictMono}). The coherent order parameter grows strictly with the coupling, which with the continuity at threshold is the qualitative bifurcation diagram.

One more step makes the equation mean in Lean what it means on paper. As a definition, \texttt{selfConsistency K D r} is just $R(Kr/D)$ --- a function of $r$ with a suggestive name, and nothing in the source says it has anything to do with an order parameter. \texttt{Phase8\_SelfConsistency}~\S8 supplies the identification. \texttt{vonMisesDensity} is the normalized weight, proved to be a genuine probability density on $[-\pi,\pi]$; \texttt{circularOrderParameter} is the average of $e^{i\theta}$ against a density, the continuum analogue of the finite-system \texttt{order\_parameter\_complex} of \texttt{Phase4}; and that average is proved to be exactly $R(a)$ --- real, because the density is even, so the mean direction vanishes. \texttt{fixedPoint\_iff\_selfReproducing} then states the equivalence the name promises: $r$ solves $r = R(K,r)$ if and only if the von Mises density it induces has order parameter $r$. Read through this, the existence theorem says that above $K_c$ there is a stationary density whose own order parameter is positive, and the solution $r = 0$ is identified with the uniform density, whose order parameter is zero.

What remains not established, and we separate it carefully. The stationary density is an \emph{input}: deriving it from the Langevin dynamics $d\theta = (\omega + K\!\cdot\!\text{mean-field})\,dt + \sqrt{2D}\,dW$ needs the Fokker--Planck operator, existence and uniqueness of its stationary solution, and a spectral stability argument, none of which Mathlib has. Of the coherent branch we prove existence, uniqueness, continuous emergence at the threshold and strict growth in $K$, but not that it is the dynamically selected one: nothing in that file mentions a trajectory, so stability and selection are not statements it can make. And while \texttt{selfConsistency} is now connected to the order parameter of a \emph{density}, it is connected to no \emph{trajectory}: \texttt{circularOrderParameter} is an integral, \texttt{order\_parameter\_complex} is an average over finitely many oscillators, and relating them is the mean-field limit --- propagation of chaos for the finite Kuramoto system --- which is a research programme rather than a lemma. That single gap is now the whole of what separates our threshold predicate from a statement about dynamics. Table 1 records the row accordingly as a theorem about the self-consistency equation from an assumed density, not as a derivation of the threshold from the dynamics. Perceptual anomalies such as visual snow can be modeled as boundary conditions where microscopic thermal noise ($D$) fails to be fully coarse-grained away by the continuous field, leaking across the $K_c$ threshold into the macroscopic gestalt.

Clinical evidence for this phase transition is observed during general anesthesia. Work by Miller and colleagues \cite{bastos2021} demonstrates that anesthetics like propofol induce unconsciousness not by suppressing neural firing, but by disrupting the phase alignment of high-frequency cortical rhythms. Destroying the dynamic stability of these rhythms breaks the structural resonance of the continuous field, forcing the integrated spatial coupling below the critical threshold $K_c$ and fracturing the unified Global Section back into disjoint, unconscious local fields.

\section{The Sleep-Inertia Timescale Mismatch}
\label{sec:prediction}
Most of what this framework asserts is shared with some rival: that cortical synchrony matters, that dissipation constrains cognition, that unity is a physical rather than a computational property. One prediction is not shared, and it follows from the framework's specific commitment --- that the medium carrying the unified state is a continuous field whose effective coupling depends on the physical geometry of the extracellular space, not on the rhythms alone. Where that commitment differs from a purely electrical account, it differs in an observable way, and we set out the difference precisely enough to be wrong.

The daily loss of consciousness during sleep is modeled here as an active physical disruption of the medium rather than a quiescence of the oscillators. During sleep, astrocytic shrinkage expands the extracellular space, allowing an influx of cerebrospinal fluid (CSF) to flood the brain for metabolic waste clearance \cite{xie2013}. This influx alters the spatial medium itself, disrupting the continuous structural resonance and preventing the coherent sheaf-theoretic gluing that conscious unity requires on this account. The oscillators need not stop for the field to fragment; the geometry they are coupled through is what changes.

The prediction concerns sudden awakenings. When jolted awake, the locus coeruleus floods the cortex with noradrenaline. High-frequency electrical rhythms attempt to phase-align in milliseconds \cite{bastos2021}, but the noradrenaline-driven astrocytic constriction required to squeeze the excess CSF out of the interstitial pathways operates on a timescale of seconds to minutes \cite{xie2013} --- three to six orders of magnitude slower. During that lag the electrical oscillators are synchronizing across a waterlogged topology, and the fragmented structural resonance is what is experienced as grogginess. The unified Global Section re-establishes only when astrocytic volume finishes matching the state of the electrical rhythms.

\paragraph{Why this implies a delayed sigmoid.} The two timescales separate cleanly, and that separation is what makes the prediction sharp rather than merely qualitative. Write $K(t)$ for the effective coupling of the continuous field, which on this account is a monotone function of the reconstricting extracellular geometry, and let $r(t)$ be the order parameter of the phase field. The electrical variable is fast: at every instant the oscillators are already at the configuration appropriate to the current $K$, so $r$ tracks the equilibrium branch $r(K)$ rather than relaxing on its own. The slow variable $K(t)$ then carries the whole time course, and it relaxes over $10^{2}$--$10^{3}$ seconds. What Derivation 7 establishes about that branch is exactly what the functional form needs, and each of the three pieces is a theorem rather than a picture: at or below the threshold the incoherent solution $r=0$ is the \emph{only} non-negative solution of the self-consistency equation (\texttt{fixed\_\allowbreak point\_\allowbreak eq\_\allowbreak zero\_\allowbreak of\_\allowbreak le\_\allowbreak critical}); above it the coherent solution exists and is unique (\texttt{supercritical\_\allowbreak fixed\_\allowbreak point\_\allowbreak existsUnique}); it emerges \emph{continuously} at $K_c$ rather than by a jump (\texttt{coherent\_\allowbreak branch\_\allowbreak continuous\_\allowbreak at\_\allowbreak threshold}); and it is strictly increasing in $K$ (\texttt{coherent\_\allowbreak branch\_\allowbreak strictMono}). A jump discontinuity at threshold would predict a step rather than a sigmoid, and a non-monotone branch would predict overshoot; neither is what the mathematics gives. Composing a monotone relaxation $K(t)$ with a branch that is identically zero below $K_c$, rises continuously from zero at $K_c$, and saturates above it yields a curve with three phases: a genuine \emph{latency} while $K(t) < K_c$, during which recovery is not merely slow but absent; a continuous rise as the threshold is crossed; and a saturation as $K(t)$ reaches its waking value. That is a delayed sigmoid, and the delay is not a fitting parameter --- it is the time the slow variable takes to reach $K_c$.

\paragraph{What the alternative predicts.} An account on which grogginess is the electrical realignment itself --- the rhythms are simply slow to re-phase --- has no slow variable and no threshold to cross. Its recovery is a relaxation toward a fixed point, and relaxation toward a fixed point is exponential near it: monotone from $t = 0$, fastest at the start, with no flat foot. The two curves are distinguishable in the first seconds, which is where they differ most, and the discriminating feature is the presence or absence of an initial interval of no recovery. This is a comparison of functional forms on data of a kind already collected in sleep-inertia studies, not a proposal for a new instrument.

\paragraph{What would falsify it.} An exponential fit that beats a delayed-sigmoid fit on the early portion of the recovery curve. A recovery latency that does not scale with an independent measure of extracellular volume or CSF clearance. A pharmacological manipulation of astrocytic volume that leaves the latency unchanged while altering the rhythms, or the converse. Any of these would tell against the framework's central empirical commitment, which is not that synchrony matters but that the \emph{medium} is what gates it.

\paragraph{What this prediction assumes.} Three things, and they are not small. The identification of the effective coupling $K$ with a monotone function of extracellular geometry is a modelling assumption, not a measurement. The threshold value $K_c = 2D$ is derived from an assumed von Mises stationary density and is not yet connected to a trajectory --- the mean-field limit is the gap, and Derivation 7 states it as such --- so the argument uses the \emph{existence and continuity} of the coherent branch at threshold, which is proved, rather than the numerical value of the threshold, which is conditional. And the prediction is about functional form, not about a time constant: it says the recovery curve has a flat foot whose width is set by the slow variable, not that the foot is any particular number of seconds long. A framework that predicted the number would be saying more; this one is not in a position to.

\section{Corollary: Hardware Thermodynamics and Topological Rigidity}
Both biological tissue and artificial silicon hardware (like GPUs) are physical dissipative structures. When a GPU computes, its charge carriers undergo physical state transitions; by Landauer's principle \cite{landauer1961}, this erasure irreversibly exhausts heat into the environment. 

There is a fundamental thermodynamic distinction in the degrees of freedom available to minimize this dissipation. In standard von Neumann architectures, the spatial topology (the silicon routing lattice) is rigidly fixed. The coupling matrix $A_{ij}$ is constrained by a discrete layout that cannot be physically altered during operation. The brain's own architecture provides empirical validation for this constraint. The cerebellum contains approximately 80\% of the brain's neurons arranged in a highly efficient, discrete, feedforward crystalline circuit \cite{herculanohouzel2009, lange1975}. Clinical observations of cerebellar agenesis report largely preserved conscious experience despite motor and cognitive deficits \cite{yu2015}, and this dissociation has been read as evidence for the importance of recurrent architecture by IIT \cite{tononi2015} --- an argument we borrow at the level of the anatomical observation while declining its $\Phi$-based explanation. Lacking the dynamic, continuous, and recursive spatial coupling of the cerebrum, it operates much like a biological GPU---highly optimized for discrete processing, yet apparently not required for conscious experience. We take the dissociation as evidence, not as confirmation: it is consistent with subjective experience being a thermodynamic attractor of continuous topologies rather than a byproduct of information processing, and it is the kind of observation that would be hard to reconcile with the converse. It is a single anatomical case read against a theory, and it bears the weight such a case can bear.

This constraint does not preclude artificial consciousness, but rather restricts its hardware. To achieve the phase transition into a unified Global Section, artificial systems must abandon the discrete rigidity of von Neumann architectures and utilize continuous physical substrates. Analog field-based computing, neuromorphic architectures with continuous memristive plasticity, and thermodynamic computing systems \cite{england2018, ganesh2021} possess the spatial coupling required to physically morph their internal topology. This aligns with Geoffrey Hinton's concept of ``mortal computation'' \cite{hinton2022}: architectures where software is inextricably fused with the physics of the hardware, meaning the knowledge dies with the physical substrate. Only hardware that computes via continuous physical dynamics can undergo the thermodynamic phase transition necessary for structural resonance.

Biological consciousness utilizes macroscopic continuous electromagnetic fields and a ``rich alphabet'' of diverse molecular and transcriptomic cell states \cite{tasic2018}. This allows the coupling matrix $A_{ij}$ to deform across a continuous parameter space. Our lemma \texttt{exists\_better\_coupling\_allocation}, wrapped as \texttt{continuous\_beats\_rigid\_topology} (\texttt{Phase7\_Hardware\-Comparison.lean}), formalizes the reallocation step of this argument: whenever a coupling matrix places weight on a less phase-correlated pair while a more correlated pair exists, there is another matrix with \emph{identical} total coupling resources that achieves strictly greater phase correlation.

Two objections to that formalization have since been answered, and we state precisely what is and is not now established. The first was that the reallocation lemma assumes its conclusion: the hypothesis \texttt{is\_strictly\_suboptimal} --- positive weight on a badly-correlated pair while a better one exists --- is most of what one wants to prove about rigid hardware. It is no longer assumed. \texttt{Phase7\_Rigidity.lean} models an architecture by the set $S$ of site-pairs it has a \emph{wire} for, and requires its couplings to vanish off $S$; weights are tunable, wiring is not. Then \texttt{totalCorrelation\_le\_of\_realizable} bounds every realizable coupling by $R\cdot M$, where $R$ is the resource budget and $M$ bounds phase correlation over the wired pairs; the bound is attained; and \texttt{rigid\_is\_strictly\_suboptimal} \emph{derives} the hypothesis whenever the substrate's best-correlated pair is one the wiring misses. \texttt{rigid\_gap} makes it quantitative: an unconstrained reallocation of the same budget gains at least $R$ times the correlation difference. \texttt{Examples.lean}~\S8 exhibits a three-site substrate wired to the one maximally anti-correlated pair, where the gap is $2$. This is resource-matched and no longer question-begging, but note what it turns on: fixed wiring support, not continuity. The second objection was that nothing in the development distinguished a continuum from a lattice --- both matrices lived on one finite index type. \texttt{Phase7\_Rigidity.lean}~\S3 gives a distinction that is genuinely typed. In the field functional of Section~\ref{sec:scaling}, which integrates a coupling kernel against $\mu \otimes \mu$, an architecture with finitely many sites is a \texttt{Finset} of points and a field-coupled region is a set of positive measure. On a substrate whose measure has no atoms, \texttt{fieldCorrelation\_sited\_eq\_zero} shows a kernel supported on finitely many sites contributes \emph{exactly zero} coupling energy, whatever weights it carries and whatever the phase field does, while a patch of strength $c$ on $U$ achieves $c\,\mu(U)^2$. \texttt{Examples.lean}~\S8 instantiates this on the real line: a thousand units of coupling concentrated at the origin register zero against a unit patch that registers one. The limit of that result is equally plain, and it is not resource-matching in the earlier sense --- a measure-zero substrate has zero resource in the $\mu\otimes\mu$ sense too. What it establishes is that the continuum coupling energy is blind to a finitely-sited architecture, which is a statement about which functional such an architecture can register in, not a demonstration that digital hardware computes worse. Taken together: the resource-matched content of the corollary is the wiring-support result, the continuity content is the measure-theoretic one, and the step from either to ``von Neumann architectures cannot experience unified consciousness'' remains an informal argument. 

The remainder of this section extends those two results informally, and we mark the transition rather than blur it. On the reading we propose, a GPU's fixed routing matrix leaves it topologically fragmented: the discrete layout does not supply the spatial coupling that sheaf-theoretic gluing into a singular Global Section requires. Without this unified topological space, rigid hardware cannot undergo the reflexive auto-resonance necessary to map its own causal boundary and generate a ``Self.'' The distinction between an LLM on a GPU and a conscious mind is not the absence of physical thermodynamics in the former, but its structural limitation in achieving topological integration. This perspective aligns with John Searle's biological naturalism \cite{searle1980, seth2024}. Where the Chinese Room argument asserted that discrete symbol manipulation cannot generate conscious understanding, our derivation supplies a candidate physical mechanism for the asymmetry --- topological rather than semantic, and, as stated above, formalized only as far as the wiring-support and measure-theoretic results reach. Asserting consciousness in fragmented, discrete GPU clusters without a unified phase transition abandons computational functionalism, relying instead on ``silicon panpsychism''—the assumption that digital computation inherently generates awareness regardless of topological integration \cite{goff2019}.

The same reasoning bears on extreme functionalist thought experiments, such as a macroscopic system of pulleys operating over millennia to implement a Large Language Model, or a human manually executing inference matrices by hand. Beyond lacking the continuous spatial topology required for sheaf-theoretic gluing, these discrete systems violate an implied temporal constraint of our theory. Structural resonance requires local oscillators to phase-align against the dissipating effects of thermodynamic noise (as modeled by our critical threshold $K_c = 2D$). A system executing state transitions over millennia, or via slow manual calculations, suffers from a terminal temporal mismatch: its dynamics are far too slow to ever phase-lock into a non-equilibrium steady state before thermal noise decoheres the macroscopic field. On this account the obstacle to pure symbol manipulation is physical rather than semantic: not that the symbols mean nothing, but that the substrate carrying them cannot hold a phase-locked field.

\section{Conclusion}
Human-like cognitive architectures do not require biological mysticism or exotic sub-quantum physics \cite{hameroff2014}. While quantum theories of consciousness often invoke delicate subatomic phenomena to explain subjective unity, every step of the chain assembled here is classical: broken symmetries, finite phase space, Landauer erasure, and gradient descent on entropy production. What the formalization establishes is that this chain is mathematically coherent and that its remaining physical commitments are localized and named --- not that classical fields are \emph{sufficient} for consciousness. Two links carry that weight, and their status is no longer the same. The unified state is proved to glue uniquely once a cover sits at the minimum of the coupling potential; the motion is proved to stop along every trajectory (\texttt{velocity\_sq\_tendsto\_zero}); and \emph{where} it stops is now proved, conditionally --- for initial data within a quarter turn and below an explicit energy threshold, the trajectory converges to a global minimiser of the coupling potential (\texttt{kuramoto\_\allowbreak tendsto\_\allowbreak global\_\allowbreak minimum}), and a cover built on that limit discharges the equilibrium hypothesis Derivation 5 used to assume (\texttt{ThermodynamicCover.of\-Convergent\-Trajectory}). What is \emph{not} proved is the unconditional statement, and it is false: splay and twisted configurations are equilibria of the same flow, so the basin condition is the scope of the claim rather than slack in it. That a physical cortex starts inside that basin is an assumption. The second link is untouched by any of this: the identification of a global section of the probability sheaf with the unity of experience is a stipulation of the framework, not a consequence of it. Subject to those two, cognitive architectures can be modeled as macroscopic attractor states of a universe governed by broken symmetries, finite phase space, and the physical imperative to minimize thermodynamic dissipation. 

Philosophically, this framework bridges the historical gap between Type Identity Theory \cite{smart1959} and Computational Functionalism \cite{putnam1967}. The subjective mental state is identical to the physical thermodynamic field (Identity Theory), yet its qualitative nature is defined by the topological and functional dynamics of structural resonance rather than the specific carbon-based biochemistry of the brain (Functionalism). This constrains the \textit{multiple realizability} of mind: consciousness is not exclusively bound to biological carbon, but neither is it substrate-neutral, because the substrate is what fixes which couplings are physically available. How far that constraint reaches is the question the corollary answers only in part. What is proved is that fixed wiring is strictly suboptimal at matched resource, and that a finitely-sited architecture registers exactly zero coupling energy in the continuum functional; the step from either to ``von Neumann architectures cannot be conscious'' is an argument we make in prose and do not formalize. 

By anchoring these derivations in Russellian Monism, this framework acknowledges the constitutive limits of third-person scientific explanation. As argued by Love \cite{love2026}, building upon Nagel's assertion that subjective phenomenal experience is inherently not amenable to objective measurement \cite{nagel1986}, the ``hard problem'' of consciousness may represent a category error rather than a solvable scientific puzzle. Science can map the physical dynamics of topological resonance and sheaf-theoretic gluing, but it is structurally incapable of adjudicating the first-person qualitative experience. Our formalization respects this boundary: the Lean 4 theorems verify the objective physical attractors, while the intrinsic ``what it is like'' to be that resonant field remains the domain of the first-person perspective. By locating the phase transition at a threshold with a closed-form value --- $K_c = 2D$ for the noisy mean-field Kuramoto model the framework adopts, conditional on the von Mises stationary density and not yet linked to a trajectory --- it aligns with what Anil Seth terms the ``real problem'' of consciousness \cite{seth2021}. Rather than attempting to bridge an unbridgeable metaphysical gap, it offers a topological architecture that can help explain, predict, and physically perturb the structural properties of experience.

Future empirical validation of this thermodynamic framework may leverage 3D cortical organoids, offering a controlled biological environment to systematically manipulate metabolic substrates and observe the topological emergence of these coherent phase transitions.

\section*{Code Availability}
The complete Lean 4 formalization, encompassing the derivation of all theorems presented in this manuscript, as well as the accompanying Python simulations, are publicly available in the GitHub repository: \url{https://github.com/bobaseb/physical_primitives_of_mind}. Additional documentation and web-accessible proofs are hosted at the project website: \url{https://bobaseb.github.io/physical_primitives_of_mind/}.

\section*{Acknowledgments}
The conceptualization, manuscript drafting, and Lean 4 formalizations presented in this work were heavily AI-assisted. The authors gratefully acknowledge the use of the Google Antigravity CLI, specifically utilizing Gemini 3.1 and Claude 4.6 Sonnet as collaborative reasoning partners throughout the research and formalization processes. In addition, DeepSeek V4 Flash and Claude Opus 5 (routed via OpenRouter) operating through Nous Research's Hermes Agent framework provided substantial assistance with code generation, proof debugging, and structural analysis.

\begin{thebibliography}{12}
 
 \bibitem[Pinotsis \& Miller(2022)]{pinotsis2022}
 Pinotsis, D. A., \& Miller, E. K. (2022). Beyond dimension reduction: Stable electric fields emerge from and allow representational drift. \textit{NeuroImage}, 253, 119058.
 
 \bibitem[Bauer et al.(2022)]{bauer2022}
 Bauer, L. G., et al. (2022). Quantification of Kuramoto Coupling Between Intrinsic Brain Networks Applied to fMRI Data in Major Depressive Disorder. \textit{Frontiers in Computational Neuroscience}, 16.
 
 \bibitem[van Bree et al.(2025)]{vanbree2025}
 van Bree, S., et al. (2025). Processes and measurements: a framework for understanding neural oscillations in field potentials. \textit{Trends in Cognitive Sciences}.
 
 \bibitem[Tononi \& Koch(2016)]{tononi2016}
 Tononi, G., Boly, M., Massimini, M., \& Koch, C. (2016). Integrated information theory: from consciousness to its physical substrate. \textit{Nature Reviews Neuroscience}, 17(7), 450-461.
 
 \bibitem[Baars(1997)]{baars1997}
 Baars, B. J. (1997). In the Theater of Consciousness: The Workspace of the Mind. \textit{Oxford University Press}.
 
 \bibitem[Dehaene \& Marti(2014)]{dehaene2014}
 Dehaene, S., Charles, L., King, J. R., \& Marti, S. (2014). Toward a computational theory of conscious processing. \textit{Current Opinion in Neurobiology}, 25, 76-84.
 
 \bibitem[Friston(2010)]{friston2010}
 Friston, K. (2010). The free-energy principle: a unified brain theory?. \textit{Nature Reviews Neuroscience}, 11(2), 127-138.
 
 \bibitem[Kuramoto(1984)]{kuramoto1984}
 Kuramoto, Y. (1984). Chemical Oscillations, Waves, and Turbulence. \textit{Springer Series in Synergetics}, Vol. 19.
 
 \bibitem[Scherf(2026)]{scherf2026}
 Scherf, M. (2026). A Formal Proof of Non-Duality: An Exposition of the 'OnlyOne' System. \textit{Unpublished manuscript, PhilPeople}.
 
 \bibitem[Inoué(2026)]{inoue2026}
 Inoué, T. (2026). On Brain as a Mathematical Manifold: Neural Manifolds, Sheaf Semantics, and Leibnizian Harmony. \textit{arXiv:2601.15320 [q-bio.NC]}.
 
 \bibitem[Spisák \& Friston(2026)]{spisak2026}
 Spisák, T., \& Friston, K. (2026). Self-orthogonalizing attractor neural networks emerging from the free energy principle. \textit{Neurocomputing}.
 
 \bibitem[Pinotsis \& Miller(2023a)]{pinotsis2023ephaptic}
 Pinotsis, D. A., \& Miller, E. K. (2023). In vivo ephaptic coupling allows memory network formation. \textit{Cerebral Cortex}, 33(17), 9877-9890.

 \bibitem[Pinotsis \& Miller(2023b)]{pinotsis2023cytoelectric}
 Pinotsis, D. A., Fridman, G., \& Miller, E. K. (2023). Cytoelectric coupling: Electric fields sculpt neural activity and "tune" the brain's infrastructure. \textit{Progress in Neurobiology}, 226, 102465.

 \bibitem[Ruffini \& Santarnecchi(2020)]{ruffini2020modeling}
 Ruffini, G., Salvador, R., Tadayon, E., Sanchez-Todo, R., Pascual-Leone, A., \& Santarnecchi, E. (2020). Realistic modeling of mesoscopic ephaptic coupling in the human brain. \textit{PLOS Computational Biology}, 16(6), e1007923.

 \bibitem[Cunha \& Lima(2024)]{cunha2024ephapticity}
 Cunha, G. M., Corso, G., de Sousa, M. P. B., \& Lima, G. Z. d. S. (2024). Can ephapticity contribute to brain complexity?. \textit{PLOS ONE}, 19(12), e0310640.

 \bibitem[Goldwyn \& Rinzel(2016)]{goldwyn2016neuronal}
 Goldwyn, J. H., \& Rinzel, J. (2016). Neuronal coupling by endogenous electric fields: Cable theory and applications to coincidence detector neurons in the auditory brainstem. \textit{Journal of Neurophysiology}, 115(4), 2033-2051.
 
 \bibitem[McFadden(2002)]{mcfadden2002}
 McFadden, J. (2002). The conscious electromagnetic information (cemi) field theory: the hard problem made easy?. \textit{Journal of Consciousness Studies}, 9(8), 45-60.

 \bibitem[Pockett(2012)]{pockett2012}
 Pockett, S. (2012). The electromagnetic field theory of consciousness: a testable hypothesis about the characteristics of conscious as opposed to non-conscious fields. \textit{Journal of Consciousness Studies}, 19(11-12), 191-223.

 \bibitem[Goff(2019)]{goff2019}
 Goff, P. (2019). \textit{Galileo's Error: Foundations for a New Science of Consciousness}. Pantheon Books.

 \bibitem[Seth(2021)]{seth2021}
 Seth, A. (2021). \textit{Being You: A New Science of Consciousness}. Penguin.

 \bibitem[Seth(2024)]{seth2024}
 Seth, A. (2024). Conscious artificial intelligence and biological naturalism. \textit{PsyArXiv}.

 \bibitem[Levin(2019)]{levin2019}
 Levin, M. (2019). The computational boundary of a ``self'': developmental bioelectricity drives multicellularity and scale-free cognition. \textit{Frontiers in Psychology}, 10, 2688.

 \bibitem[Friston \& Kiebel(2009)]{friston2009}
 Friston, K., \& Kiebel, S. (2009). Predictive coding under the free-energy principle. \textit{Philosophical Transactions of the Royal Society B: Biological Sciences}, 364(1521), 1211-1221.

 \bibitem[Clark(2016)]{clark2016}
 Clark, A. (2016). \textit{Surfing Uncertainty: Prediction, Action, and the Embodied Mind}. Oxford University Press.

 \bibitem[Hohwy(2013)]{hohwy2013}
 Hohwy, J. (2013). \textit{The Predictive Mind}. Oxford University Press.

 \bibitem[Prigogine \& Stengers(1984)]{prigogine2018}
 Prigogine, I., \& Stengers, I. (1984). \textit{Order Out of Chaos: Man's New Dialogue with Nature}. Bantam Books.

 \bibitem[McFadden(2020)]{mcfadden2020}
 McFadden, J. (2020). Integrating information in the brain's EM field: the cemi field theory of consciousness. \textit{Neuroscience of Consciousness}, 2020(1), niaa016.

 \bibitem[Fr\"{o}hlich \& McCormick(2010)]{frohlich2010}
 Fr\"{o}hlich, F., \& McCormick, D. A. (2010). Endogenous electric fields may guide neocortical network activity. \textit{Neuron}, 67(1), 129-143.
 
 \bibitem[Anastassiou \& Koch(2015)]{anastassiou2015}
 Anastassiou, C. A., \& Koch, C. (2015). Ephaptic coupling to endogenous electric field activity: why bother?. \textit{Current Opinion in Neurobiology}, 31, 95-103.
 
 \bibitem[Pockett(2002)]{pockett2002}
 Pockett, S. (2002). Difficulties with the electromagnetic field theory of consciousness. \textit{Journal of Consciousness Studies}, 9(4), 51-56.
 
 \bibitem[V\"or\"oslakos et al.(2018)]{voroslakos2018}
 V\"or\"oslakos, M., et al. (2018). Direct effects of transcranial electric stimulation on brain circuits in rats and humans. \textit{Nature Communications}, 9, 483.

 \bibitem[Tasic et al.(2018)]{tasic2018}
 Tasic, B., et al. (2018). Shared and distinct transcriptomic cell types across neocortical areas. \textit{Nature}, 563(7729), 72-78.

 \bibitem[Verkhratsky \& Nedergaard(2018)]{verkhratsky2018}
 Verkhratsky, A., \& Nedergaard, M. (2018). Physiology of Astroglia. \textit{Physiological Reviews}, 98(1), 239-389.

 \bibitem[Picard \& Shirihai(2022)]{picard2022}
 Picard, M., \& Shirihai, O. S. (2022). Mitochondrial signal transduction. \textit{Cell Metabolism}, 34(11), 1620-1638.

 \bibitem[Harnad(1990)]{harnad1990}
 Harnad, S. (1990). The symbol grounding problem. \textit{Physica D: Nonlinear Phenomena}, 42(1-3), 335-346.

 \bibitem[Tononi \& Koch(2015)]{tononi2015}
 Tononi, G., \& Koch, C. (2015). Consciousness: here, there and everywhere? \textit{Philosophical Transactions of the Royal Society B: Biological Sciences}, 370(1668), 20140167.

 \bibitem[Aaronson(2014)]{aaronson2014}
 Aaronson, S. (2014). Why I Am Not An Integrated Information Theorist (or, The Unconscious Expander). \textit{Shtetl-Optimized}. Retrieved from \url{https://scottaaronson.blog/?p=1799}.

 \bibitem[Block(1995)]{block1995}
 Block, N. (1995). On a confusion about a function of consciousness. \textit{Behavioral and Brain Sciences}, 18(2), 227-247.

 \bibitem[Herculano-Houzel(2009)]{herculanohouzel2009}
 Herculano-Houzel, S. (2009). The human brain in numbers: a linearly scaled-up primate brain. \textit{Frontiers in Human Neuroscience}, 3, 31.

 \bibitem[Lange(1975)]{lange1975}
 Lange, W. (1975). Cell number and cell density in the cerebellar cortex of man and some other mammals. \textit{Cell and Tissue Research}, 157(1), 115-124.

 \bibitem[Yu et al.(2015)]{yu2015}
 Yu, F., Jiang, Q. J., Sun, X. Y., \& Zhang, R. W. (2015). A new case of complete primary cerebellar agenesis: clinical and imaging findings in a living patient. \textit{Brain}, 138(6), e353.

 \bibitem[Bastos \& Miller(2021)]{bastos2021}
 Bastos, A. M., Donoghue, J. A., Brincat, S. L., Mahnke, M., Yanar, J., Correa, J., ... \& Miller, E. K. (2021). Neural effects of propofol-induced unconsciousness and its reversal using thalamic stimulation. \textit{eLife}, 10, e60824.

 \bibitem[Hameroff \& Penrose(2014)]{hameroff2014}
 Hameroff, S., \& Penrose, R. (2014). Consciousness in the universe: A review of the 'Orch OR' theory. \textit{Physics of Life Reviews}, 11(1), 39-78.

 \bibitem[Hunt \& Schooler(2019)]{hunt2019}
 Hunt, T., \& Schooler, J. W. (2019). The easy part of the hard problem: a resonance theory of consciousness. \textit{Frontiers in Human Neuroscience}, 13, 378.

 \bibitem[England(2018)]{england2018}
 England, J. L. (2018). Dissipative adaptation in driven self-assembly. \textit{Nature Nanotechnology}, 13(4), 287-295.

 \bibitem[Ganesh \& Parrondo(2021)]{ganesh2021}
 Ganesh, N., \& Parrondo, J. M. R. (2021). Thermodynamic computing with non-equilibrium systems. \textit{Nature Physics}.

 \bibitem[Hinton(2022)]{hinton2022}
 Hinton, G. (2022). The Forward-Forward Algorithm: Some Preliminary Investigations. \textit{arXiv preprint arXiv:2212.13345}.

 \bibitem[Searle(1980)]{searle1980}
 Searle, J. R. (1980). Minds, brains, and programs. \textit{Behavioral and Brain Sciences}, 3(3), 417-424.

 \bibitem[Smart(1959)]{smart1959}
 Smart, J. J. C. (1959). Sensations and brain processes. \textit{The Philosophical Review}, 68(2), 141-156.

 \bibitem[Putnam(1967)]{putnam1967}
 Putnam, H. (1967). Psychological predicates. In \textit{Art, Mind, and Religion}. University of Pittsburgh Press.

 \bibitem[Xie \& Nedergaard(2013)]{xie2013}
 Xie, L., Kang, H., Xu, Q., Chen, M. J., Liao, Y., Thiyagarajan, M., ... \& Nedergaard, M. (2013). Sleep drives metabolite clearance from the adult brain. \textit{Science}, 342(6156), 373-377.

 \bibitem[Nagel(1986)]{nagel1986}
 Nagel, T. (1986). \textit{The View from Nowhere}. Oxford University Press.

 \bibitem[Love(2026)]{love2026}
 Love, B. C. (2026). Consciousness, AI, and the Limits of Scientific Explanation. \textit{arXiv preprint arXiv:2606.00226}.

 \bibitem[Norton(2005)]{norton2005}
 Norton, J. D. (2005). Eaters of the lotus: Landauer's principle and the return of Maxwell's demon. \textit{Studies in History and Philosophy of Science Part B: Studies in History and Philosophy of Modern Physics}, 36(2), 375-411.

 \bibitem[Landauer(1961)]{landauer1961}
 Landauer, R. (1961). Irreversibility and heat generation in the computing process. \textit{IBM Journal of Research and Development}, 5(3), 183-191.
 
 \bibitem[Kibble(1976)]{kibble1976}
 Kibble, T. W. B. (1976). Topology of cosmic domains and strings. \textit{Journal of Physics A: Mathematical and General}, 9(8), 1387-1398.
 
 \bibitem[Sakaguchi(1988)]{sakaguchi1988}
 Sakaguchi, H. (1988). Cooperative phenomena in coupled oscillator systems under external fields. \textit{Progress of Theoretical Physics}, 79(1), 39-46.
 
 \bibitem[Barbalat(1959)]{barbalat1959}
 Barb\u{a}lat, I. (1959). Syst\`{e}mes d'\'{e}quations diff\'{e}rentielles d'oscillations non lin\'{e}aires. \textit{Revue Roumaine de Math\'{e}matiques Pures et Appliqu\'{e}es}, 4, 267-270.
 
 \bibitem[Still et al.(2012)]{still2012}
 Still, S., Sivak, D. A., Bell, A. J., \& Crooks, G. E. (2012). Thermodynamics of prediction. \textit{Physical Review Letters}, 109(12), 120604.
 
 \bibitem[Kawai et al.(2007)]{kawai2007}
 Kawai, R., Parrondo, J. M. R., \& Van den Broeck, C. (2007). Dissipation: The phase-space perspective. \textit{Physical Review Letters}, 98(8), 080602.
 
 \bibitem[Banach(1922)]{banach1922}
 Banach, S. (1922). Sur les op\'{e}rations dans les ensembles abstraits et leur application aux \'{e}quations int\'{e}grales. \textit{Fundamenta Mathematicae}, 3(1), 133-181.
 
 \bibitem[Coddington \& Levinson(1955)]{coddington1955}
 Coddington, E. A., \& Levinson, N. (1955). Theory of Ordinary Differential Equations. \textit{McGraw-Hill}, New York.
 
 \bibitem[Cover \& Thomas(2006)]{cover2006}
 Cover, T. M., \& Thomas, J. A. (2006). Elements of Information Theory (2nd ed.). \textit{Wiley-Interscience}, Hoboken, NJ.
 
 \bibitem[Shenker(2000)]{shenker2000}
 Shenker, O. R. (2000). Logic and entropy. \textit{Philosophy of Science}, 67, S146-S152.

 \end{thebibliography}

\end{document}
