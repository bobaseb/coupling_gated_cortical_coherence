/.lake
# Python
__pycache__/
*.py[cod]
.venv/
.mypy_cache/
.ruff_cache/

# LaTeX
*.aux
*.log
*.out
# No BibTeX runs here: references.tex is an inline `thebibliography`, so a
# .bbl/.blg pair on disk is a leftover from a toolchain this project no longer
# uses, not a build input.
*.bbl
*.blg
*.pdf
# ...but PDFs the README or the project website (index.html) link to directly
# are deliverables and must be tracked.
!main.pdf
!supplementary.pdf
!docs/primer.pdf
*.toc
*.fls
*.fdb_latexmk
*.synctex.gz
arxiv_submit/

# Local agent/tool state
.hermes/
