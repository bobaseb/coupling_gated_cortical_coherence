/.lake
# Python
__pycache__/
*.py[cod]
.venv/
.mypy_cache/
.ruff_cache/

# LaTeX
*.aux
*.log
*.out
*.pdf
# ...but the two published PDFs are the deliverables: README and the project
# website (index.html) link to them directly, so they must be tracked.
!main.pdf
!supplementary.pdf
*.toc
*.fls
*.fdb_latexmk
*.synctex.gz
arxiv_submit/
